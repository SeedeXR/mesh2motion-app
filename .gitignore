# dependencies
**/node_modules/**

# build output
dist/
legacy/dist/
target/
**/target/
*.app

# rust
**/*.rs.bk
Cargo.lock.bak

# tauri
src-tauri/gen/

# macOS
.DS_Store

# tooling
.scannerwork/
coverage/
bench/results/
*.trace

# env
.env
.env.local

# Mixamo reference animations used for FBX round-trip testing.
# NOT committed: Mixamo assets are royalty-free to use but are not CC0, and this
# repo's art assets are all CC0 (see LICENSE-CC0.MD). Keeping them local avoids
# redistributing content under a licence the repo does not claim.
# Contributors supply their own; see memory/test.md §4.
references/
.sonar-token

# packaged app artifacts (scripts/build.sh)
/output/

# playwright artifacts
/e2e/*.png
/test-results/
/playwright-report/
