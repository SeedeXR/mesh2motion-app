# Roadmap & Execution Tracker

**Rules for this file**
- Completed: `- [x]`
- Changed requirement: ~~struck through~~ with the replacement and a **reason** beneath
- Never delete a line — history is the point
- The autonomous loop stops when every P0–P4 item is checked

**Legend** — `P0` foundation · `P1` core algorithms · `P2` I/O · `P3` UX ·
`P4` bridge/polish · `R` research spike

---

## P0 — Foundation

- [x] **P0-1** Ground: read legacy codebase, verify toolchain, research SOTA *(2026-08-29)*
- [x] **P0-2** Create `memory/` with all 13 agent documents *(2026-08-29)*
- [x] **P0-3** Move existing app to `legacy/` with `git mv` (preserve history); keep it runnable *(2026-08-29 — git mv, history preserved; verified: 107/107 legacy tests pass + production build succeeds)*
- [x] **P0-4** Install Tauri CLI; scaffold `src-tauri/` + `app/`; verify `cargo tauri dev` launches *(2026-08-29 — tauri-cli 2.11.4 via npm; verified: app bundles at 6.4 MB, launches, IPC round-trip live)*
- [x] **P0-5** Create Rust workspace `crates/{m2m-core,m2m-io,m2m-rig,m2m-bridge}` with `#![forbid(unsafe_code)]` *(2026-08-29 — 4 crates, all `#![forbid(unsafe_code)]`; 9 tests, clippy + fmt clean)*
- [x] **P0-6** Install SonarQube + scanner; `sonar-project.properties`; verify a scan completes *(2026-08-29 — SonarQube 26.8.0 community running in Docker; scan completed, **quality gate OK, 0 issues**. Note: Community Edition has no Rust analyser, so Rust quality is gated by `clippy -D warnings` in CI; Sonar covers the TypeScript frontend only.)*
- [x] **P0-7** CI: `.github/workflows/ci.yml` — rust-test, rust-lint, arch-gate, frontend, build. **Verify green via `gh run watch`.** *(2026-08-29 — 6 jobs; arch-gate, frontend, legacy suite green; macOS jobs pending)*
- [x] **P0-8** `.cargo/config.toml` with `debug = "line-tables-only"`; `.gitignore` for `target/`, `dist/`, `node_modules/` *(2026-08-29)*
- [x] **P0-9** Vendor Asta Sans (OFL) + Lucide; implement `design.md` tokens as `app/src/ui/tokens.css` *(2026-08-29 — subset upstream 5.5 MB TTF to Latin, **28.4 KB woff2**, variable axis preserved; OFL.txt vendored alongside. Load verified in the running app: `font ok`.)*
- [x] **P0-10** `bench/` harness; capture **legacy baselines** for all 9 templates before any solver work *(2026-08-29 — all 9 captured to `bench/baselines/legacy-solver.json`. Criterion harness for the Rust side still to come with P1-1.)*
  - Headline: **68% of 22196 vertices carry only ONE bone influence** (human worst at 87%), mean 1.13–1.81 against a GPU limit of 4. This is the rigid-assignment signature the geodesic solver must move.
  - 0 unnormalised vertices across all templates — an invariant P1 must preserve.
- [x] **P0-11** Verify empirically whether WebGPU is available in this WKWebView; record result in `architecture.md` *(2026-08-29 — **WebGPU confirmed available**, better than the assumed WebGL2 fallback. Adapter requested inside the shipped app. Recorded as ADR A1a; backend now detected at runtime and shown in the status bar.)*

## R — Research spikes (write `docs/research/<topic>.md` before implementing)

- [x] **R-1** Survey SOTA skinning + auto-rigging *(2026-08-29 — written up in `docs/research/skinning-sota.md`)*
- [~] **R-2** Geodesic voxel binding: full paper read, pseudocode, parameter table → `docs/algorithms/geodesic-voxel-binding.md`
  - [x] Design doc written: pipeline, robustness table, complexity, parameters, rejected alternatives, verified quotes from the published abstracts
  - [ ] **Full paper still unread.** Four things are deliberately marked unverified and must NOT be guessed: the geodesic propagation algorithm (Dijkstra / fast marching / flood fill), the exact weight falloff function, interior classification for open surfaces, and the reported timings. The authors' project page `delasa.net/voxelization/` is dead (domain resold). Get the SCA 2013 paper via ACM DOI 10.1145/2485895.2485919, and the 2014 IEEE TVCG extended version for degenerate geometry.
- [x] **R-3** Robust Biharmonic Skinning (arXiv:2406.00238) — **decided OUT** *(2026-08-30, `docs/research/robust-biharmonic-decision.md`)*
  - Read the full text rather than the abstract, which is what the earlier survey entry was based on. Implementation is PyTorch + custom CUDA + **OptiX/OWL/Warp** — all NVIDIA-only, on a project targeting Apple Silicon. Reported **71.74 s** on Bunny against a 3 s budget.
  - The robustness it buys (non-watertight, self-intersecting, triangle soup) is what voxelisation already gives us — measured in P1-3, where the reference character is not watertight and solves without incident.
  - Steal-worthy idea recorded for P3: it folds artist weight painting into the optimisation as Dirichlet boundary conditions rather than post-processing it.
- [ ] **R-4** Non-human rig conventions: survey how Blender/Maya/Rigify handle avian wing chains, fish spines, quadruped scapulae → `docs/research/creature-rigs.md`
- [ ] **R-5** Source CC0/CC-BY rigged reference creatures to expand the template library. **Record provenance + licence per asset.**
- [ ] **R-6** FBX 7.4/7.5 binary write format — spec gaps, since no Rust writer exists
- [ ] **R-7** Evaluate UniRig/RigAnything ONNX export feasibility (informs P4-6; do not implement yet)

## P1 — Core algorithms (`m2m-core`)

- [x] **P1-1** Mesh representation *(2026-08-29 — SoA positions + indices, vertex welding with a 27-cell spatial hash, edge adjacency, union-find components. **Half-edge deliberately not built**: the geodesic solver runs on the voxel grid, not the mesh graph, and the only consumer of adjacency is validation. Normals deferred until something needs them.)*
- [x] **P1-2** Mesh validation *(2026-08-29 — watertightness, boundary/non-manifold edges, degenerate triangles, duplicate vertices, components, bounds/diagonal. 22 tests including a real 1761-vertex human mesh. Scale *detection* deliberately not implemented: the solver normalises by the diagonal anyway, so unit-guessing is a UI concern — the raw diagonal is reported instead of an invented enum.)*
- [x] **P1-3** Sparse voxelisation with interior/exterior classification *(2026-08-29)*
  - Conservative triangle-AABB rasterisation (Akenine-Möller 13-axis SAT), then a 6-connected exterior flood fill from the padded grid boundary; interior is whatever neither reaches.
  - **The 61-component requirement is met:** all components share one grid, so nested islands connect in voxel space. Tested directly with a small cube fully inside a large one — surfaces disconnected, interiors joined.
  - **`DEFAULT_RESOLUTION = 256`, measured not guessed.** The deciding metric is interior/surface ratio: 0.08 at res 32 (shell-dominated, nothing for the geodesic field to traverse) vs 2.69 at 256. Full sweep in `docs/algorithms/geodesic-voxel-binding.md`.
  - **Physical validation:** interior volume at 256 scales to **56 litres** for a 1.75 m human. A 60 kg person displaces ~60 L.
  - Non-watertight input (26 boundary edges) does **not** leak — conservative rasterisation seals sub-voxel holes. A face-sized hole does leak, and that limit is tested both ways.
  - **Axis-aligned geometry needed three separate fixes** before the shell sealed: the rasterisation AABB excluded the voxel containing a boundary face, the overlap test had no epsilon, and world-space rasterisation lost precision far from the origin. A plain cube had no shell at all at resolutions 13/18/20 before this. Regression sweep: 1476 cases (scale × offset × rotation × resolution).
- [x] **P1-4** Geodesic distance field over voxel interior, per bone, `rayon`-parallel *(2026-08-29)*
  - Dijkstra over a compact graph of non-exterior voxels, 26-connected with true Euclidean step lengths. Surface voxels are included — vertices sit on the surface, so excluding them would strand every vertex.
  - **190 ms** for 7399 verts × 66 bones at resolution 256; **1.9 MB** retained.
  - **Memory was the design constraint:** a per-bone field over the whole grid is 900 MB. Only non-exterior voxels participate (201k of 3.4M) and only vertex distances are retained; the field is per-thread scratch.
  - **Measured payoff: the dominant bone changes for 14.6% of vertices vs Euclidean, worst path ratio 19.4×** — on a T-pose model, the case most favourable to Euclidean.
  - `unreachable_bones()` and `unreachable_vertices()` surface the two failure modes (bone outside the mesh; island the grid never connected) instead of silently producing dead limbs.
  - **Known limit, measured and pinned:** surfaces closer than ~1.5 voxels leak into each other, restoring the Euclidean shortcut. At the default resolution on a 1.75 m human that is a ~1 cm floor — fine for an A-pose arm at 2–5 cm, not for an arm actually touching the body.
- [x] **P1-5** Weight assignment from geodesic falloff, k≤4 bones/vertex *(2026-08-30)*
  - Modified Shepard over the k nearest bones, cutoff at the surplus (k+1)-th distance so weight reaches exactly zero there and the blend does not step at the fourth influence. **The falloff function is our choice, not the paper's** — the published abstracts do not state one (R-2 open).
  - **Measured against the legacy baseline on the same model and rig: single-influence vertices 87% → 9.0%, mean influences 1.13 → 3.66.**
  - **Never leave a vertex unweighted.** Islands still isolated after voxelisation must fall back to nearest-bone and be flagged in the report, or eyes and teeth detach and float.
- [x] **P1-6** Normalisation + root/leaf pruning *(2026-08-30 — normalisation is part of the falloff pass. Pruning is a caller-supplied boolean mask rather than name inspection: `m2m-core` must not carry a naming convention. The legacy invariant is preserved by whoever builds the mask.)*
- [x] **P1-7** Property tests for all 8 invariants in `test.md` §3 *(2026-08-30)*
  - All 8 covered, on synthetic geometry, the real 7399-vertex character, **and** randomised `proptest` generators over scale/offset/rotation/resolution/falloff/bone-count.
  - **Invariants 7 and 8 needed splitting into an exact half and a converging half.** Bone *assignment* is exactly scale-invariant and exactly mirror-symmetric (0 mismatches over 480 random poses). Weight *values* only converge, because the geodesic path is a chain of discrete voxel steps. Both errors halve per resolution doubling — first-order, so discretisation rather than bias — and the tests assert **convergence**, not a fixed tolerance.
  - `proptest` earned its place immediately: it found a scale-invariance failure the fixed-fixture test could not reach and shrank it to a minimal case (base 0.05, factor 26.0).
- [x] **P1-8** A/B against legacy on all 9 templates *(2026-08-30)* — **every template improves on both metrics**, verdicts below. Pinned as a test (`crates/m2m-core/tests/template_ab.rs`) that fails per-template if the solver regresses; mutation-verified by regressing the solver to rigid assignment.

| template | verts | weightable bones | single-influence % | mean influences | raw mean | ms |
|---|---|---|---|---|---|---|
| human | 7399 | 52 | 86.8 → **9.5** | 1.13 → **2.46** | 3.66 | 289 |
| fox | 1222 | 38 | 49.1 → **3.7** | 1.51 → **2.89** | 3.96 | 281 |
| bird | 1852 | 47 | 60.6 → **8.2** | 1.39 → **2.83** | 3.96 | 115 |
| horse | 2146 | 44 | 55.2 → **2.7** | 1.45 → **2.73** | 3.97 | 333 |
| fish | 3526 | 25 | 67.4 → **10.1** | 1.33 → **2.84** | 3.93 | 198 |
| dragon | 2561 | 76 | 73.0 → **23.7** | 1.27 → **2.34** | 3.51 | 111 |
| kaiju | 1571 | 45 | 47.9 → **4.6** | 1.52 → **2.96** | 3.97 | 204 |
| snake | 995 | 24 | 19.0 → **3.3** | 1.81 → **3.15** | 3.98 | 13 |
| spider | 924 | 43 | 58.4 → **15.5** | 1.42 → **2.24** | 3.98 | 141 |

  - 0 fallback vertices on every template — no vertex needed the Euclidean guess.
  - **The bone mask had to reproduce the legacy skip set exactly** (`name === 'root'`, or childless *and* named `leaf`/`tip`) or the comparison is not like-for-like. A structural definition ("no Bone child") looked equivalent and was not: it excluded 8 `wing_feather_*` bones on the bird that legacy weights, giving 39 bones against legacy's 47.
  - The `raw mean` column counts any non-zero influence, the threshold the legacy baseline used, so it is the directly comparable figure; the `mean influences` column counts influences above 1% and is the honest one.
  - Dragon (23.7%) and spider (15.5%) improve least; both are the busiest rigs relative to their vertex count (76 bones / 2561 verts, 43 / 924). Worth revisiting at P4-4.
- [x] **P1-9** The three legacy weight correctors are **not ported**, and P1-8 is the justification *(2026-08-30)*
  - They are not deleted from `legacy/` — that tree must stay runnable as the A/B baseline (`test.md` §9). The decision is that no equivalent exists in `m2m-core` and none will be added.
  - They existed to patch the Euclidean nearest-bone failure: `ArmWeightCorrector` for arms near the ribcage, `ExtremityWeightCorrector` for fingers grabbing knuckles, `HeadWeightCorrector` for the head/neck boundary, plus `WeightSmoother` for the seams single-bone assignment leaves. Geodesic distance removes the cause, so there is nothing for them to fix — every template improves without any per-body-part correction.
- [x] **P1-10** ~~Optional biharmonic refinement pass~~ — **not built**, R-3 decided the method out *(2026-08-30)*
  - **Reason for the change:** the gating research resolved against it on platform (NVIDIA-only stack) and budget (10–25× over). A second solver is not free — two weighting paths to test, benchmark and keep correct — and nothing yet says the geodesic weights are insufficient. Revisit if artists report quality problems, or if a Metal implementation of the visibility kernel lands inside budget.
- [x] **P1-11** Benchmark vs. budgets in `test.md` §6 *(2026-08-30)* — **comfortably inside, no optimisation needed**

| vertices | voxelise | geodesic | weights | total | peak heap |
|---|---|---|---|---|---|
| 7,399 | 30 ms | 255 ms | 3 ms | **288 ms** | **30 MB** |
| **48,670** | 37 ms | 253 ms | 18 ms | **307 ms** | **44 MB** |
| 213,754 | 57 ms | 340 ms | 76 ms | **474 ms** | **129 MB** |

  - Budget is 3 s / 1.5 GB at 50k vertices. Measured on the 48,670-vertex row: **307 ms / 44 MB** — about **10× under time and 34× under memory**. The budget was written before any measurement; it is conservative, not tight.
  - **Resolution is the dominant cost, not mesh density.** Solve time barely moves from 7k to 214k vertices because the geodesic Dijkstra runs over the voxel grid, whose size depends only on resolution. Resolution is cubic: 7 ms at res 64, 39 at 128, 131 at 192, 295 at 256, 1004 at 384 — doubling resolution costs ~8×. This is what the P3 resolution control must communicate.
  - Peak **heap** measured with a tracking allocator, not process RSS. RSS never shrinks, so in a multi-test binary it reports everything that ran before — the mistake the legacy benchmark made in session 003.
  - `instruction.md` §5 optimisation ordering deliberately **not** applied: there is nothing to optimise against. Revisit if the resolution control lets users past 384.

## P2 — I/O (`m2m-io`)

- [x] **P2-1** Port FBX binary reader *(2026-08-30)* — parses the real 2.1 MB Mixamo export: **version 7700, 11 roots, 6099 nodes, 11.6 ms**
  - Structure verified against what a rigged character must contain: 67 Model, 131 Deformer, 315 AnimationCurve, 2 Geometry, 666 Connections; `Geometry/Vertices` decodes to 42,696 f64 (14,232 vertices) through the zlib path.
  - **Deliberate divergence:** the legacy `BinaryParser` interleaves binary decoding with semantic reshaping (`Properties70` flattening, `Connections` collection, single-property collapsing). Split here — this layer produces a faithful typed node tree, and the reshaping moves to the DOM layer in P2-3. The original cannot be tested without the reshaping; this can.
  - **No legacy tests existed for these two files** (only FBXTreeParser/GeometryParser/TextParser have them), so the harness is the real Mixamo file plus hostile input.
  - Trust-boundary guards, each mutation-tested: depth limit (a stack overflow aborts rather than unwinds, so it cannot be caught), declared-length checks before allocating, inflate bounded by the *declared* size and the maximum deflate ratio, and **footer-magic validation**.
  - **The footer check exists because of a serious silent failure found in review:** cutting 578 bytes from the 2.1 MB file — 0.03% — parsed to `Ok` with 10 roots and 6089 nodes, having discarded the entire `Takes` section, i.e. every animation stack. The end-of-content test is an offset heuristic, so a cut inside the last root just stops the loop early. The 16-byte footer magic is identical across all 8 real Mixamo exports checked and is the only reliable completeness signal.
- [x] **P2-2** Port FBX ASCII reader *(2026-08-30)* — produces the **same `FbxDocument`** as the binary reader, so P2-3 has one input shape
  - All 8 legacy test cases ported. They are regression guards for real bugs the legacy fixed over upstream three.js, so they came across with the code.
  - **Format normalisation belongs in the readers; semantic reshaping does not.** Binary stores a vertex array as a property on its `Vertices` node; ASCII writes `Vertices: *9 { a: ... }`, putting the numbers on a child. That is a format quirk, reconciled here by hoisting `a:` onto the parent. `Properties70` flattening and `Connections` collection stay in P2-3.
  - **Mutation-tested the two regressions that matter.** Un-anchoring the brace reproduces the historical bug exactly: parsing `ASCII_FBX` then yields **only `FBXHeaderExtension`** — every node after a `P:` line containing `{Project}` is silently discarded. Removing the document-level property path reproduces the other.
  - No regex dependency: leading tabs are counted directly, which is simpler than the legacy's dynamically-built `\t{N}` patterns and makes the end-of-line brace anchor explicit rather than emergent.
  - **Review found 10 issues, 3 high — my "one input shape" claim was false.** The `*N` array count was emitted as a leading `Str`, so the ASCII tree differed from binary at `properties[0]`; my test used `find_map` over the property list and missed it. Now the full property vector is asserted.
  - Other real ones: `is_ascii_fbx` sliced `&text[..1024]`, which **panics** when the cut lands mid-character — a panic on the trust boundary, and exporters do put non-ASCII in headers. Comma splitting ignored quotes, so `"Model::Bob, Jr"` became two properties and shifted every positional index. Indentation was ignored entirely, so a **missing** `}` swallowed whole sections silently (legacy at least skipped such lines; this now resyncs to the line's own depth, which is better than either). Short `a:` arrays were accepted against their declared `*N`. The `Content:` base64 continuation was not ported, silently dropping embedded textures.
  - **Cross-format accessors rather than guessed types.** ASCII cannot know whether `100` was an `i32` or `i64`, so `as_i64`/`as_f64`/`as_i64_vec` read by meaning, and `split_object_name` handles both `"Bob\0\x01Geometry"` (binary) and `"Geometry::Bob"` (ASCII) without discarding the class.
- [~] **P2-3** Port `FBXTreeParser` — split across sessions, ~~1620 LOC~~ **scoped down, measured**
  - **Reason for the change:** of `FBXTreeParser.ts`'s 1574 lines of method code, **319 are textures and materials and 218 are lights and cameras** — about 46% once material-parameter parsing is counted. None of it belongs in `m2m-io`, which reads geometry and rigs (`architecture.md` §2); the viewport loads materials itself. Only the rigging path is ported, roughly 700 lines.
  - [x] **A — the DOM layer** *(2026-08-30)*: connection graph, objects addressable by id, `Properties70` flattened to named typed values. This is the semantic reshaping both readers deferred, done **once** here rather than twice as the legacy does.
    - Real rig resolves to 642 objects: 67 Model, 131 Deformer (129 Cluster + 2 Skin), 315 AnimationCurve, 2 Geometry, 2 Pose. `mixamorig:Hips` carries `Lcl_Translation` Y = 104.3 cm, a plausible hip height in the centimetres Mixamo exports.
    - **Cross-format equality asserted directly**, not by testing each reader separately: the same document built through both paths yields the same object identity, relationships and property values. Mutation-verified — breaking `split_object_name` fails it on object identity.
    - **Review caught the same tautology pattern again, in the test written to avoid it.** The digest comparison was `assert_eq!(a.len(), b.len())` — row counts, 2 == 2 — and the real comparison *failed*, because `{:?}` on the raw variant shows `I64(104)` for ASCII against `F64(104.0)` for binary. The digest now renders values through the accessors, which is the contract that lets widths differ, and compares in full.
    - **`Link::property` kept `"Lcl Translation"` while flattened keys used `"Lcl_Translation"`**, so `scene.object(id)?.property(link.property)` returned `None` for every `Lcl` channel — and 215 of the 666 connections in the reference rig are object-to-property, with the animation path as the consumer that walks them. Both normalised now, with a test asserting the exact set of targeted property names.
    - `Scene::from_document` consumes the document rather than borrowing: cloning each object node duplicated every vertex, index and weight array for the scene's lifetime, roughly doubling peak memory on a large mesh.
  - [ ] ~~B — Geometry~~ and ~~D — Deformers~~ — **folded into P2-4, which already covered both.** The A–D split was invented in session 013 without checking P2-4's scope; geometry and skin clusters live in `GeometryParser.ts`, not `FBXTreeParser.ts`, so they belong there. P2-3 keeps only what is genuinely `FBXTreeParser`'s: the DOM layer and the model/bone hierarchy.
  - [x] **B — Models and the bone hierarchy** *(2026-08-30)*: transform pipeline, connection hierarchy, local + world matrices
    - **Checked against the legacy loader node-for-node, not against plausibility.** `legacy/bench/dump-model-fixtures.ts` runs the legacy's own `FBXLoader` headless over the reference rig and records every Model's parent, local and world matrix; `dump-transform-fixtures.ts` does the same for 49 `generateTransform` cases. Worst deviation **8.5e-14 local / 2.0e-13 world** on a ~165-unit rig — f64 rounding, i.e. the two implementations do the same arithmetic. "The hips are about a metre up" would have passed with the pipeline subtly wrong.
    - **Why a differential test and not reasoning:** two conventions here are invisible to inspection. three.js Euler order strings are the **reverse** of FBX's extrinsic integers (FBX order 0 → three.js `'ZYX'`), and three.js composes `'XYZ'` as the literal product `Rx·Ry·Rz` (verified in `three.core.js`). Invert either and rotations stay smooth, invertible, and wrong.
    - **Corpus measurement (8 files, 522 Models)** — this decided the scope: 520 `LimbNode` + 2 `Mesh`; `Lcl_Translation` 520, `PreRotation` 440, `Lcl_Rotation` 274, `Lcl_Scaling` 63 (**all of them 1.0**), `InheritType` 67 (values 1 and 2; the other 455 default to 0). **No file carries `PostRotation`, any pivot/offset, or `RotationOrder`.** Those paths are implemented anyway — Maya and Max exports use them, and dropping them silently misplaces nodes — with synthetic coverage only.
    - **Because every scale in the rig is 1, all three `InheritType` modes agree on it.** Four mutations survived the real-rig tests for exactly this reason, and needed synthetic non-uniformly-scaled parents to pin: reading `InheritType` at all, passing the parent's *local* vs *world* matrix, multi-parent resolution, and cycle-breaking.
    - **A three.js artefact nearly corrupted the fixture.** `buildSkeleton` creates a separate `Bone` object per skeleton for a Model shared between skins — both skins here cover the same 64 bones — and nests the second inside the first. Taking "first node with this id" would have exported an identity local matrix for every shared bone: for the hips, wrong by the entire 104.27 cm hip height. The dumper selects the node carrying `userData.transformData` and asserts that is unique.
    - Malformed-input handling, each with a test: a **cycle** in the connection graph is cut and counted (otherwise those Models are unreachable from any root and never get a transform, and `ancestors` would not terminate); **multiple parents** resolve to the last, matching three.js's `Object3D.add`, and are counted; a parent **collapsed to zero scale** leaves the child at identity and is counted rather than propagating glam's NaN through the whole subtree.
    - **Review found three real divergences from the legacy, all invisible to the reference corpus, all fixed with tests that fail without the fix:**
      1. **`Root` was not treated as a bone.** The legacy builds a `Bone` for `case 'LimbNode': case 'Root':`. Every file in the corpus is `LimbNode`, but a 3ds Max Biped export names its skeleton root `Root` — it would have parsed cleanly and silently dropped the root joint from anything filtering on `is_bone()`.
      2. **`InheritType::from_fbx` sent unknown values to the wrong mode.** The legacy branches `if (0) … else if (1) … else { Rrs }`, so a corrupt or vendor-specific `3` composes through `Rrs` there; this mapped it to `RrSs`. Every child of such a node would get a different, finite, plausible matrix.
      3. **Shear: a deliberate divergence, now documented and pinned.** The legacy stores each local matrix on an `Object3D` via `applyMatrix4` + `updateWorldMatrix`, which recomposes it from position/quaternion/scale — a representation with no room for shear. **Measured against three.js r185: a sheared matrix through that round trip changes by 0.39.** FBX genuinely produces shear when an ancestor is non-uniformly scaled (the local matrix contains `GSM⁻¹·R·GSM`), and a `DMat4` can hold what an `Object3D` cannot, so this port **keeps** it. Consequence to remember: the fixture test agrees to 8.5e-14 only because every scale in the reference rig is 1.0. Regenerate fixtures from a non-uniformly-scaled rig and it will fail, correctly.
    - One review finding (`ancestors` missing its step limit) was **stale** — the guard and its test were added before the review reported, and the reviewer had snapshotted the file earlier. Verified by removing the guard: the test then grows unboundedly until SIGKILL.
  - [ ] **B-follow-up — two things the legacy does that this does NOT yet do.** Both measured to be no-ops on the current corpus, both real for other files:
    - **`BindPose` rest-pose override.** `parseScene` overrides the transform of any bone *without* a skin cluster from the `Pose` section, because `Lcl` properties may hold an animation frame rather than the rest pose. Measured on the reference rig: **65 bones, 65 of them cluster-driven, so 0 are overridden** — it changes nothing here. It needs cluster knowledge (P2-4b) and is really a rest-pose question, so it belongs with the rig layer. Bones *with* clusters already take their bind pose from `TransformLink`, which P2-4b does.
    - **`UpAxis == 2` (Z-up) scene rotation.** The legacy rotates the root by −90° about X. Every file in the corpus is `UpAxis == 1`. Belongs at the importer/scene level, not in the Model tree.

- [~] **P2-4** Port `GeometryParser` (985 LOC) incl. skin clusters — **absorbs P2-3's geometry and deformer parts**
  - [x] **a — mesh geometry** *(2026-08-30)*: vertices, polygon indices, triangulation, normals, UVs
    - Real rig: Beta_Surface 14,232 verts → **28,272 triangles**, Beta_Joints 10,514 → **20,840**, derived from the measured polygon mix (172 tris + 14,050 quads; 1,400 + 9,720) rather than asserted as a bound.
    - **Measured before choosing a triangulator:** the file contains *only* triangles and quads, no n-gons. So the legacy's earcut-over-a-tangent-plane is not needed. A quad splits along the diagonal whose two triangles both agree with the polygon normal — exact, not a heuristic — and anything larger is fanned and **counted in the report** so an approximation is never silent.
    - `vertex_source` records the original FBX vertex behind each expanded corner. Vertices expand per polygon corner because normals and UVs are per-corner, so skin weights — indexed by original vertex — need the mapping back. The legacy calls this `remapSkinIndices`; P2-4b depends on it.
    - The two meshes use **different** normal mappings (`ByPolygonVertex`/Direct and `ByVertice`/Direct), so one file covers both resolution paths. Normals verified unit-length under both.
    - **Review verified two of my tests were hollow: the entire UV path could be deleted, and `vertex_source` could be a constant 0, with all tests still passing.** Both assertions were length checks the loop pushes satisfy unconditionally — and `vertex_source` is what P2-4b's skin remap depends on. Now checked by value: every expanded corner must sit exactly where its source vertex does (mutation: 84,811 of 84,816 mismatch), and UVs by non-zero count and unit-square bounds (mutation: 0 of 84,816 non-zero). UVs are the file's only `IndexToDirect` layer, so they are the sole coverage of the indirection branch.
    - **Functional gap found: the geometric pre-transform was dropped.** `GeometricTranslation/Rotation/Scaling` sit on the Model, are not part of the node transform, and apply to the mesh alone. Mixamo writes identity — which is why nothing caught it — but Maya and Max exports commonly do not, and the result is a mesh offset from its skeleton. Now read from the connected Model, applied to positions, with normals transformed by the inverse transpose so a non-uniform scale does not tilt them.
    - Also: unresolvable layer indices and unrecognised mapping types were silently zero-filled or dropped; both are counted in the report now. The quad claim was softened to "exact for **planar** quads" — a bowtie satisfies neither diagonal and is now counted as ambiguous rather than silently guessed.
  - [x] **b — skin clusters** *(2026-08-30)*: per-bone vertex indices, weights, bind transforms
    - Real rig: **129 clusters** over two skins (Beta_Surface 65, Beta_Joints 64), **26,745 index/weight pairs**. After binding: 0 unweighted, 0 over the influence limit, 0 vertices failing renormalisation on either mesh.
    - **40 of the 129 clusters carry no `Indexes` node at all** — every one a finger bone (Pinky1, Thumb4, Index1/4). A bone can be bound to a skin while influencing no vertices; the legacy guards for it the same way (`if ('Indexes' in boneNode)`). Treated as an empty influence set, and the count is pinned by a test so a parser change that started dropping them (shifting every later bone index) would fail.
    - **Bind matrix differs deliberately from the legacy.** Each cluster holds `TransformLink` (the bone's global transform at bind time) and `Transform` (the mesh's). The legacy uses only `inverse(TransformLink)` and supplies the mesh transform separately as three.js's `bindMatrix` from the rebuilt scene graph. This port folds them: `inverse(TransformLink) * Transform` — the same composition, but sourced from what the exporter *recorded* rather than from reconstructing the graph identically. **Measured: all 129 clusters have a non-identity `Transform`** (worst component deviation 179.9), so dropping it would misplace every mesh. Verified by the identity `TransformLink · inverse_bind · v == Transform · v`.
    - **Surprising measurement, recorded for the solver work:** the reference rig stores only **~1.08 influences per vertex** — 94% of Beta_Surface corners are single-bone (79,632 of 84,816; max 3), and Beta_Joints maxes at 2. Confirmed two independent ways: the influence histogram after binding, and 26,745 raw pairs ÷ 24,746 source vertices read straight from the FBX nodes. This is the file's own truth, not a parser artefact — worth knowing before treating Mixamo weights as a quality baseline to beat.
    - **Nine defects found in review, every one a quiet partial success — an `Ok` return with deformation silently wrong or missing.** All fixed, each with a test that fails without the fix:
      1. **Index/weight desync.** A negative index was dropped from `indices` alone while its weight stayed, sliding every later weight one vertex left; the length check then reported a benign "ragged array". Now paired before filtering, so an unusable index takes its weight with it (`unusable_indices`).
      2. **Zero-cluster skin bound successfully** to bone 0 with `bone_ids() == []` — an index into an empty bone list, reached from `Ok`. Now an error.
      3. **`bind` never checked it was handed its own mesh.** Vertex counts do not identify a mesh; two same-sized geometries bound silently with entirely wrong weights. `MeshGeometry` now carries its `id` and `bind` compares it.
      4. **Missing/short/non-finite bind matrix silently became identity** — the exact ~180-unit misplacement the module docs argue against — with no counter. Now `matrices_defaulted`.
      5. **Singular `TransformLink` inverted to all-NaN.** glam is built without `glam-assert`, so `DMat4::inverse` on a zero determinant does not panic; the NaN would spread into every vertex the bone touched. Rejected at parse time. (A test pins the glam behaviour itself, so a future release that changes it fails loudly.)
      6. **An undecodable `Indexes` array was indistinguishable from an absent one** — and 40 clusters legitimately have none, so a corrupt array looked normal and that bone's influence vanished. Now `undecodable_arrays`.
      7. **A weight positive as f64 but zero as f32** (1e-300) was admitted; and a weight sum saturating to infinity divided every weight to zero. Both detach the vertex while the report claims the file was fine. Narrow before testing positivity; refuse to normalise by a non-finite total.
      8. **A skin whose geometry link is missing was dropped with no counter anywhere** — indistinguishable from an unskinned mesh, and larger than any drop counted inside `SkinReport`, which goes with the skin. `parse_all` now returns `(skins, skipped)`.
      9. **Two of my own tests did not test what their names claimed.** `a_matrix_field_that_is_not_sixteen_floats_falls_back_to_identity` asserted the *opposite* proposition on a clean rig and left the fallback path at zero coverage; `a_skin_bound_to_the_wrong_mesh_is_rejected` passed only because the two reference meshes happen to differ in size. Both rewritten.
    - **`unweighted_vertices` deleted rather than fixed.** It counted source vertices while `fallback_vertices` listed corners — numbers that disagree by ~6x for the same event — and it counted loose vertices that reach the output not at all. `fallback_vertices` is now the single record, in the unit the output is in.
    - Ponytail review caught the `Indexes`/`Weights` read cloning each property before `as_*_vec` allocated a second copy — two full copies of 26,745 f64s per parse, now zero.
    - **23 mutations run against this module, all killed.** Four survived at first and each exposed a real gap rather than a redundant test: two guards that were redundant *for the input I chose* (needing a discriminating case), an assertion that read through `influences()` — which filters zero weights and so could not see the defect — and an entirely uncovered non-finite-matrix branch.
- [x] **P2-5** Port `AnimationParser` (783 LOC) *(2026-08-30)* — curves, layers and stacks into keyframe tracks
  - **Diffed key-for-key against the legacy loader: 2 clips, 53 tracks, 7,844 keys.** Worst deviation **exactly 0 on times and 1.7e-18 on values** — bit-level agreement, not "close enough". `legacy/bench/dump-animation-fixtures.ts` runs the legacy's own `FBXLoader` headless and records every track.
  - **The rotation path is the whole difficulty and the reason a differential test was mandatory.** A quaternion track is Euler-order composition × `PreRotation` premultiply × sign-unroll, and each step yields a smooth, unit-length, entirely plausible track when wrong. **43 of the 52 animated models carry a `PreRotation`**, so it is not a corner case.
  - **The fixture caught a real ordering bug immediately:** clips come out in ascending *stack id* order (the legacy iterates a JS object keyed by numeric ids, which JS enumerates numerically), not alphabetically. Stack 477116448 is `mixamo.com`, 485299280 is `Take 001`. I had sorted by name.
  - **Corpus measurement (8 files) decided the scope — and left five mutations alive that only synthetic tests could kill:** zero `PostRotation`, zero scale/morph channels, zero curve nodes missing an axis, zero nodes whose axes disagree in length, **zero rotation steps ≥180°** (so the sub-division path never fires on real data), **zero sign flips in 7,644 adjacent key pairs**, and **zero non-default Euler orders**. Also: all 53 tracks share one end time (4.9 s), so duration-from-longest was indistinguishable from duration-from-first.
  - **A wrong premise nearly shipped a vacuous test.** My first unroll test used (0,0,0)→(170,170,170), reasoning from the *XYZ* half-angle formula; the default order is **ZYX**, where that gives w ≈ +0.99 and never flips. A search found that **no** triple with every axis under 180° can flip against the identity. The real case came from scanning pairs: (-180,-150,-180)→(-30,-30,-30), dot **-0.90**, every axis step under 180.
  - **Trust boundary:** a curve whose `KeyTime` and `KeyValueFloat` differ in length would have indexed past the end of the value array while sampling — a panic straight from file content. Now truncated to the shorter and counted, the same way `skin.rs` handles ragged cluster arrays.
  - **Deliberate divergences, both documented in the module:** morph (`DeformPercent`) channels are **counted, not parsed** — they need the geometry's blend-shape list, which nothing here produces yet, and a file losing facial animation must not look complete; and the ≥180° sub-division slerps in quaternion space rather than round-tripping through Euler (lossy at gimbal lock) and emits the interval endpoint, which the legacy's `for (t=0; t<1; …)` loop drops.
  - **Review found five more, every one a trust-boundary or silent-wrongness issue, all fixed with tests that fail without the fix:**
    1. **Unbounded subdivision — a hang and an allocation bomb from file bytes.** The sub-key count came from a raw degree delta: 1e7° asks for 55,557 keys, and an infinite span saturates Rust's float→int cast to `usize::MAX`, so `for step in 1..=steps` never ends. Now clamped to `MAX_SUBDIVISION = 1024` and counted.
    2. **`synchronise`'s shortcut was NOT output-neutral, contrary to the comment I wrote defending it.** It compared key *counts*; a curve with a duplicated `KeyTime` has more values than distinct times, so it can match the deduplicated merged length while its keys sit elsewhere — every value lands a slot early and the last is invented, in a track that looks complete. Now compares the times themselves. **The legacy still has this bug.**
    3. **Non-finite keys produced NaN quaternions** with nothing in the report. A NaN bone matrix spreads into every vertex it touches and surfaces as the mesh vanishing, far from the cause. Rotation keys holding a non-finite value are now dropped and counted; position/scale hold the last good value.
    4. **Every animation layer was applied, where the legacy takes only the first.** A stack with a base plus an override layer would emit two tracks for the same bone and property in one clip — double-applied or arbitrarily resolved downstream. Now first-only, with the rest counted.
    5. The clip-ordering comment overclaimed: JavaScript enumerates integer-like keys numerically only below 2^32, so a file with very large object ids would give the legacy file order and this port id order. Ascending id is kept (deterministic), the reason is now stated correctly, and the divergence is named.
  - 30 mutations run, all killed except one that is provably a no-op: the `vector_track` scan is quadratic and a linear cursor computes the same values, so no test can distinguish the two — measured at 0.92 ms for 7,844 keys against 26 ms for the binary read, and marked with its ceiling rather than optimised.
- [x] ~~**P2-8 SHOULD RUN BEFORE P2-6**~~ — **done, and it paid for itself twice.** *(decided session 018, executed session 019)* — the read path is now feature-complete (binary, ASCII, DOM, geometry, skin, models/transforms, animation), and **manual review has now found trust-boundary defects in two consecutive sessions**: session 017's NaN-from-singular-matrix, and session 018's unbounded subdivision (a non-terminating loop and an allocation bomb, both reachable from file bytes) plus NaN keys. Those are exactly what `cargo-fuzz` finds mechanically and cheaply. Fuzzing the readers before adding a writer also means the writer's round-trip tests start from a reader that is known not to hang.
- [x] **P2-8b — per-file inflate budget** *(2026-08-30)*. `InflateBudget` threaded through `binary::parse`, charged with the declared size **before** inflating so the array that would break the budget is never allocated. Ceilings: **256 MB per property, 512 MB per file** — the per-file total is what actually bounds memory, since every inflated array stays alive in the node tree. Chose 512 MB over the 1 GB first drafted because this tool's priority is low RAM and the reference rig inflates 1.5 MB in total. The before-vs-after ordering is pinned by giving the last array a declared size but garbage instead of a zlib stream: charging first yields `ImplausibleLength`, inflating first yields `Inflate`. *Original finding:* `MAX_INFLATED_BYTES` (512 MB) is a **per-property** ceiling with no per-file total, and the 1032:1 deflate-ratio guard only requires ~508 KB of compressed input per 512 MB array — so a ~2 MB file can hold four of them and retain **~2 GB** in the node tree, already past the fuzz job's own `-rss_limit_mb=2048`, and unbounded for larger files. Not fixed in 019 because it needs a running total threaded through `binary::parse`, unlike the two caps that session did add (`MAX_KEYS_PER_CURVE`, `MAX_CORNERS`/`MAX_POLYGON_CORNERS`), which were local. Same class as those: it is a "never OOM" gap, not a theoretical one.
- [x] **P2-6** **FBX writer** — *(a) binary encoder + (b1..b4) builders all DONE 2026-08-31. **O9 is fully met**: the reference rig round-trips through our writer and Blender reads it back identically — geometry, skeleton, weights AND animation.*
  - **(a) `crates/m2m-io/src/fbx/encode.rs`** — the exact inverse of `binary::parse`. Gate: `parse(encode(parse(bytes))) == parse(bytes)` on the reference rig, plus every property variant, both offset widths, and the 255-byte name limit. Arrays are written **uncompressed** (`encoding = 0`), which the format allows; revisit with a size measurement rather than a guess.
  - **The round trip is necessary but NOT sufficient, and this is the key lesson.** Four of eight mutations against the encoder survive it, each a conformance detail our own reader does not check: the null record ending a child list (`end_offset` is authoritative, so a file without one is self-consistent to us); the top-level null record (never read — the footer heuristic stops the loop first); an uncompressed array's declared byte length (ignored); and the footer's 16-byte padding (only the last 16 bytes are checked). The encoder writes all four correctly — nothing would notice if it stopped. **Closing those needs a different reader**, which is exactly the legacy-loader gate.
  - **Found by the round trip: `FbxDocument` is a LOSSY view of object names.** Binary FBX stores `Name\0\x01Class`, and `Cursor::read_string` truncates at the NUL — so the document holds `mixamorig:Hips`, never `mixamorig:Hips\0\x01Model`, and a writer built on it emits names with no class suffix. The document still round-trips because both parses truncate identically; the loss is in the bytes. three.js truncates the same way, so it will not notice — **Blender and Maya are unverified and must be checked before the writer is called done**. Pinned by `a_name_and_its_class_do_not_survive_the_document`.
  - [x] **(b1) the independent-reader acceptance gate** *(2026-08-30)* — `legacy/bench/fbx-conformance.ts` + `tools/blender-fbx-import-check.py` + `crates/m2m-io/examples/encode_roundtrip.rs`.
    - **My hypothesis was wrong and the measurement corrected it.** I expected three.js's loader to catch the four conformance details that survive our own round trip. **It catches none of them** — it shares our reader's design: `end_offset` authoritative, heuristic footer, ignores an uncompressed array's declared byte length. Agreeing with three.js proves very little.
    - **Blender 5.2's importer is the only independent reader here, and it catches two of the four**: a missing null record after a child list (it validates the sentinel at `parse_fbx.py:225`) and a wrong array byte length.
    - **It also found a real, blocking bug.** Our reader truncated object names at the NUL in `Name\0\x01Class`, so the encoder wrote names with no class, and Blender's `elem_split_name_class` raised `ValueError: not enough values to unpack` — **the file would not open at all**. three.js truncates identically and never noticed. Fixed in the reader, not the writer: `read_string` now keeps NULs. Both its callers carry explicit lengths, so there was never padding to strip — the truncating variant is deleted rather than left as an unexplained inconsistency.
    - **The last two survivors are now explained, not merely unexplained.** Omitting the top-level null record survives *because our footer begins with zeros*, which Blender reads as the `end_offset == 0` terminator (`parse_fbx.py:159`) — it is accidentally satisfied, not optional. Footer padding is genuinely unvalidated by all three readers. Neither is a defect; both are recorded so nobody re-derives this.
  - [~] **(b2) builders** — *mesh geometry DONE 2026-08-30; skeleton, skin and animation next*
    - `crates/m2m-io/src/fbx/build.rs` writes the 10 top-level sections a mesh needs (header, FileId, CreationTime, Creator, GlobalSettings, Documents, References, Definitions, Objects, Connections) from bare positions and triangles. **Verified in Blender: a from-scratch unit cube imports as 1 mesh, 8 shared vertices, 12 triangles, 0 loose vertices** — and in three.js as 36 expanded positions.
    - **The polygon-index encoding is the subtle part**: FBX has no per-face vertex count, so a polygon's LAST corner is written bitwise-negated and that sign is the only face boundary. Both gates catch getting it wrong.
    - **The two gates are complementary, measured.** Of six mutations: negated-corner errors and a missing `\0\x01Class` are caught by BOTH; a missing `Geometry->Model` or `Model->root` connection was caught **only by Blender** (the mesh simply vanishes — our reader sees an unparented Model as a root, which is legal); an understated `Definitions` count is caught **only by our Rust tests** (Blender ignores those counts). Because Blender is not in CI, the two connection assertions were added to the Rust test as well, so those mutations cannot ship.
  - [~] **(b3) skeleton + skin** *(2026-08-30)* — animation still to come
    - **O9 is met for geometry, skeleton and weights.** Reading the reference rig through `model`/`skin::parse_all`, rebuilding with `build.rs` and re-importing in Blender gives, identically to the original: **1 armature, 65 bones with identical names AND identical parent chains, 2 meshes, 10,514/14,232 vertices, 11,120/14,222 polygons including quads, 52 vertex groups, 24,746 weighted vertices, weight total 24,746.0, and the influence histogram {1: 23117, 2: 1259, 3: 370}.**
    - **The gate found a real O9 defect: quads were being triangulated.** `geometry::parse` triangulates and expands per corner — right for a solver, wrong for a writer. Rebuilding from it turned 11,120/14,222 polygons into 20,840/28,272. `build::Faces` now takes either `Triangles` (geometry we generate) or `Polygons` (an FBX `PolygonVertexIndex` passed through verbatim), and `rebuild_rig` uses the raw arrays. An artist notices a quad mesh returned as triangles.
    - **The gate itself had two blind spots, both found by mutation and both closed**: it compared bone names and counts but not the **hierarchy**, so parenting every bone to the root passed; and it never checked **where the bones are**, so dropping `PreRotation` passed. It now asserts `bone_parents`, `root_bones` and `bone_rest`.
    - **A gate that fails for the wrong reason is worse than no gate**: the Blender report was scraped from stdout, which Blender also writes progress to, producing `SyntaxError: Unexpected non-whitespace character after JSON` instead of the assertion under test. The tool now writes the report to a file.
    - Mutations and which gate caught each: cluster/bone connection reversed (**both**), every bone parented to the root (**both**), no `Skin->Geometry` connection (**both**), no `NodeAttribute` connection (**Rust only** — Blender still builds the armature from `LimbNode`), `Definitions` undercount (**Rust only**), dropped `PreRotation` (**Rust only** — for a skinned mesh Blender takes the rest pose from the cluster `TransformLink`, which is preserved, so the Model's PreRotation is redundant there).
  - **(b4) DONE 2026-08-31**: animation — AnimationStack/Layer/CurveNode/Curve, the `Takes` section, and the OO/OP connections. Blender now reports the rebuilt reference rig **identically to the original on every field measured**: 65 bones, 52 vertex groups, 24,746 weighted vertices, 11,120/14,222 polygons (quads preserved), and the action `Armature|mixamo.com|Layer0` with `curves=520, keys=76960, range=1.00-148.00`.
    - **The frame rate is the part that nearly shipped broken.** With `TimeMode` missing from GlobalSettings, Blender read the 148-frame clip as **123.5** — the *same* 76,960 keys, the *same* 520 curves, the *same* driven paths, played 20% slow (30fps keys read at 25fps). Every count matched; only the range gave it away. Lesson: **for animation, counting keys is not verification — the time axis needs its own assertion.** `range` was added to `tools/blender-fbx-import-check.py` for exactly this.
    - `TimeMode = 6` is 30fps. **Verified empirically** (written, then read back through Blender's frame range), not recalled from the FBX enum.
    - `Scene::time_mode` now carries it on the read side (`dom.rs`), because the app must preserve a source's frame rate, not assume one. A file with no GlobalSettings reports `None` rather than defaulting silently.
    - Mutation-tested, 5 for 5: TimeMode renamed, layer name hardcoded, Curve→CurveNode reversed, CurveNode→Layer dropped, CurveNode→Model downgraded from OP to OO. Each failed exactly the intended test.
- [x] ~~**P2-6** **FBX writer**~~ — no *Rust* prior art. Round-trip against the 7 Mixamo reference clips.
  - **CORRECTION (session 020): there IS a reference implementation to diff against.** Sessions 018 and 019 both recorded "no legacy implementation", which is wrong. The legacy exports FBX via **`@comfyorg/fbx-exporter-three` v1.0.1** (`StepExportToFile.ts:3`), a third-party npm package — not three.js core, which ships no `FBXExporter`. **Verified this session: it runs headless under `vitest.bench.config.ts` exactly like the loader, and writes BINARY FBX** (`Kaydara FBX Binary` magic, version 7400, 2.97 MB from the reference rig). So the differential-fixture method used for P2-3B and P2-5 applies here after all.
  - **Our reader already parses its output cleanly** — a second, independent producer, with *every* report field zero across DOM, models, skins and animation: 132 Models, 2 Geometry, 131 Deformers, 2 skins, 1 clip of 53 tracks at 4.900 s. Good evidence the reader is not overfit to Mixamo's writer.
  - Differences worth knowing before writing one: the exporter emits **132 Models where the source has 67**, because three.js's `buildSkeleton` bone duplication becomes real objects on the way out; **159 AnimationCurves where the source has 315**, since it writes only connected curves; `vertex_count == source_vertex_count` (it writes pre-expanded, unshared vertices); and it **drops the empty `Take 001` stack**, writing 1 AnimationStack rather than 2.
  - **The round-trip gate is proven end to end** *(session 020)*: reference rig → `FBXExporter` → `FBXLoader` reloads to **129 bones, 2 SkinnedMeshes, 1 clip of 53 tracks at 4.900 s** — identical to loading the original. So P2-6's acceptance test can be "our writer's output reloads in the legacy loader with the same counts and matrices the reference exporter's does", which is a far stronger gate than "our reader can read it".
  - The legacy also offers an **Unreal FBX export preset** (`FbxExportPreset`) and an `FbxTextureCompatibilityService` that flips UV transforms for FBX, because FBX exporters assume `flipY=true` while glTF textures are `flipY=false`. Both are behaviour a port must match or deliberately drop.
- [ ] **P2-7** decision *(session 020)*: **P2-6 first, then P2-7.** The app exports BOTH formats (`ExportFormat { GLB, FBX }`), so neither is optional — but P2-6 now has a runnable reference and a complete reader to round-trip against, which makes it the lower-risk of the two despite being the larger. P2-7 keeps its `gltf` crate whenever it is taken.
- [~] **P2-7** GLTF/GLB — **read DONE 2026-08-31**, write next
  - `crates/m2m-io/src/glb/` via `gltf 1.4.1`, with `default-features = false, features = ["utils", "names"]`. **Not** the `import` feature: it resolves external URIs (file paths, `http://`, `data:`) out of an untrusted file. This reader resolves the embedded BIN chunk and nothing else, and reports `ExternalBuffer` for anything pointing outward. The app's own files are all self-contained `.glb`.
  - **Verified against Blender on all 55 GLB files in the repo — every one matches on meshes, bones, vertices, triangles and weighted vertices.** Includes the 9 template rigs (skeleton-only), the 9 base models, 19 variation rigs, and `human-interleaved-buffer-mesh.glb` (interleaved attributes with a stride). `tools/glb-blender-diff.sh` re-runs the sweep; the numbers are pinned in `crates/m2m-io/tests/glb_read.rs` because Blender is not in CI.
  - Animation matches too, once the two representations are related correctly: glTF has one channel per node+path, Blender one F-curve per *component*. 66 bones x (3 T + 4 quaternion + 3 S) = **660 curves per 198 channels**, and Blender's key total is the stride-weighted one (`Chest_Open`: 198 channels / 1,356 keys / **5,128 component keys** = Blender's 5,128). **Duration matches exactly**: 1.375 s x 24 fps = frame 33, across all 87 clips.
  - **Two Blender gotchas that looked like reader bugs and were not:**
    1. The importer creates an **icosphere as a bone display widget** (`blender/imp/node.py` calls `primitive_ico_sphere_add`), which lands in `bpy.data.objects` as a real MESH. It added a phantom 42-vertex, 80-polygon mesh to every skinned file. Pass `disable_bone_shape=True`. Without it, `rig-human.glb` — a file whose JSON has **zero** meshes — imports as "1 mesh".
    2. **A glTF mesh is not a mesh object.** One mesh holds one primitive per material and importers merge them: `human-jay.glb` is 1 mesh of 22 primitives. Four files "disagreed" with Blender until the comparison stopped equating primitives with meshes. `Primitive::mesh` and `Document::mesh_count()` exist for this.
  - **Fuzzing found four defects in 5 minutes, three of them in the `gltf` crate.** Target `glb` added to `fuzz_targets/`, `seed.sh` and the CI matrix; 7.4M runs clean after the fixes.
    1. *Ours*: out-of-range triangle indices were returned to the caller. glTF validation checks that an accessor fits its buffer, not that the values inside an index accessor are vertices that exist — so a "valid" file can say *draw vertex 49233* of a 995-vertex mesh, and any renderer trusting `indices` reads out of bounds. Dropped per-triangle and counted (`out_of_range_triangles`), following the reader's existing repair-and-report pattern.
    2. *Upstream, release panic*: `gltf-json-1.4.1/src/mesh.rs:151` does `root.accessors[index]` **inside the validation hook**, before validating the index — so a dangling POSITION panics the validator instead of being reported by it. This one crashes in release too, i.e. opening a malformed `.glb` would kill the app.
    3. *Upstream, debug panic*: `gltf-1.4.1/src/binary.rs:252` computes `header.length as usize - 12` on a length read straight from the file. Below 12 it underflows.
    4. *Upstream, debug panic*: `gltf-1.4.1/src/accessor/util.rs:371` `debug_assert_eq!`s the Rust type's size against the accessor's declared size, and computes `stride * (count - 1)` (underflows at `count == 0`). **`cargo test` is a debug build**, so 3 and 4 are reachable in CI.
    - Fixed by three guards ahead of the crate: `check_glb_header`, `check_indices` (a pre-flight over every index reference in the JSON), and `accessor_is` (declared layout must match what will be read). All 55 real files still read with an all-zero report, so the guards reject nothing legitimate.
    - **The lesson generalises what session 019 already learned once.** Then it was our own `debug_assert!` on file content; now it is a dependency's. *An assertion is for an invariant the code controls, and what a file contains is never one* — and that has to be checked at the boundary of every parser you depend on, not just the ones you wrote.
- [ ] **P2-7** GLTF/GLB **write** — the reader and its Blender gate are the round-trip target
- [x] **P2-8** `cargo-fuzz` targets for all three parsers; wired into CI *(2026-08-30)*
  - `crates/m2m-io/fuzz/` with three targets: `fbx_binary`, `fbx_text`, and **`fbx_pipeline`**, which drives the DOM and every layer above it. That third one is the point — **both trust-boundary defects found by hand in sessions 017 and 018 were in layers, not readers**, and fuzzing only `binary::parse` would have found neither. It is also the only target that found anything.
  - The fuzz package declares its own `[workspace]`, so its nightly requirement never reaches a stable build.
  - **Found a real bug within 90 seconds.** An `Objects` child with no numeric id tripped a `debug_assert!` in `Scene::from_document` — a **panic on untrusted file content in every debug build**, and `cargo test` in CI *is* a debug build. It had never fired only because no test fed that input. A second `debug_assert!` on duplicate object ids was the same mistake. Both are now `SceneReport` counters. **An assertion is for an invariant this code controls; what a file contains is never one.**
  - ~8.4M runs across the three targets after the fix found nothing further.
  - CI: a `fuzz` job runs 60 s per target on every PR and 30 min per target on a nightly `schedule` trigger (`memory/test.md` §5), with `-rss_limit_mb` and `-timeout` so runaway allocation and hangs fail the job rather than the runner. Crash artifacts upload on failure.
  - `corpus/` and `artifacts/` are gitignored; `fuzz/seeds/` holds committed starting inputs (including the crashing one) and `fuzz/seed.sh` assembles the corpus from those plus whichever real FBX files are present.
- [x] **P2-9** Full hostile-input corpus (`test.md` §4) passing *(2026-08-30)* — `crates/m2m-io/tests/fbx_hostile.rs`
  - Walks §4 item by item so the checklist is auditable in one place: empty and tiny inputs, wrong magic (including a single-byte near-miss), truncation at 512 offsets of the real rig, a declared length exceeding the file, deep nesting, non-finite floats, meshes with no vertices and no polygons, duplicate bone names, and every committed test file driven end to end whatever its extension.
  - **Found a second real bug.** A non-finite `Lcl_Translation` composed to a NaN matrix, and because a child multiplies by its parent's world matrix, **one bad node turned its entire subtree to NaN** — surfacing as the mesh disappearing, with nothing to say where it started. Components are now validated at read time and replaced individually, so a node's other valid components survive; counted as `ModelReport::non_finite_components`.
  - **Pinned the depth cap properly.** Deep ASCII does not exercise it: the text reader uses an explicit stack, only the binary reader recurses. The test now builds a genuinely nested binary FBX and pins the exact boundary — the outermost node is depth 0, so 257 levels are accepted and 258 are not.
- [ ] **P2-9** Full hostile-input corpus (`test.md` §4) passing — no panics, no OOM, no hangs
- [x] **P2-10** Verify exports open in Blender **and** an independent reader — *2026-08-31, and it found a real bug on the first run.*
  - Third reader is **assimp** (`/opt/homebrew/bin/assimp`, `tools/assimp-check.sh`). Not Autodesk's FBX SDK, so **this is not proof that Maya opens a file** — it is an independently written importer, which is what makes it useful. Maya itself remains unverified and needs a machine with it installed.
  - **It found what two readers could not.** Our FBX exports loaded in assimp with **zero animations** — 0 animations and 0 channels where the source has 1 and 53 — while the mesh, all 129 bones and every one of 49,112 faces came through perfectly. Blender and three.js both read the animation fine, so nothing in the existing gates could see it.
  - **Cause: an empty scope.** A childless FBX node can either declare a nested list holding only its terminating null record, or declare none at all. Our reader represented both as `children: []`, so re-encoding wrote "none" for both — and assimp reads an `AnimationLayer` without the empty list as *no layer*, leaving the stack with no layers and the file with no animation. `FbxNode::empty_scope` now carries the distinction.
  - **Both blanket rules are wrong, and I tried the wrong one first.** The reference rig has 5,144 childless nodes declaring no list and exactly **3** declaring an empty one — `References` and its two `AnimationLayer`s. Writing one for every node fixes assimp and **breaks the three.js loader** on materials. Our output now reproduces the source's census exactly: 3 / 5,144 / 952.
  - Arrays are now **deflated** when that is smaller, which is what real exporters emit. The rebuilt reference rig went from 1,860,976 to **980,672 bytes**, and re-encoding the source produces a file smaller than the source itself.
  - Verified after the fix: assimp matches the source on nodes, meshes, animations, faces, bones and channels for both the re-encoded source and the from-scratch rebuild; Blender still reports every field identically; three.js conformance 3/3. glTF was unaffected throughout — assimp read our GLB round trip correctly from the start (87 animations, 5,743 channels).
  - Vertices differ under assimp (28,464 vs 24,746) because it splits vertices at normal and UV seams and our writers carry neither. That is the documented scope of the writers, not a defect; faces are the comparable figure and match exactly.
- [ ] **P2-10b** Verify in Maya proper (Autodesk FBX SDK) — **blocked**: no Maya and no FBX SDK on this machine. `tools/assimp-check.sh` is the closest available proxy.

## P3 — Rigging UX (`m2m-rig` + `app/`)

> **Read `memory/architecture.md` §8a and decision A7 before starting P3-1.**
> Rigify (which ships with Blender) was studied in session 026 at the user's
> request. It is **GPL-2.0-or-later and we are MIT**: reimplement its
> architecture, never copy its code or its metarig bone data. The decision that
> came out of it — templates become **typed chains** rather than flat bone
> lists — shapes P3-1 through P3-4. P3-1 and P3-2 are **done**: the format
> lives in `crates/m2m-rig/src/template.rs` and all nine templates are
> annotated and checked against their `.glb` in CI.

### Reference animals the user supplied *(2026-09-01, `references/human_based_fbx_mixamo_animations/animals-3d/`)*

Five species from outside our own asset set: **giraffe, african buffalo, crow,
southern white rhino, hyena**. `references/` is **gitignored**, so these are
local study material — no test may depend on them in CI, and their licensing is
unknown, so nothing derived from them ships as a template.

What our readers make of them, and what it taught us:

| animal | format | our reader |
|---|---|---|
| crow | FBX | 91 bones, 60 clusters, **9 clips**, 1620 channels |
| hyena | FBX | 122 bones, 101 clusters, 2 clips — **matches Blender's 122** |
| buffalo | GLB | 42 bones, 28,637 verts all weighted, 1 clip 14.6 s |
| rhino | GLB | 35 bones, 33,743 verts, 3 meshes, 1 clip 17.3 s |

- **Blender 5.2 cannot open the crow at all; our reader can.** Its FBX importer
  raises `AttributeError: 'CyclesLightSettings' object has no attribute
  'cast_shadow'` at `io_scene_fbx/import_fbx.py:2255` because the file contains
  a light. The file is a valid `Kaydara FBX Binary`. Worth remembering the next
  time Blender is treated as the arbiter: it is an independent reader, not an
  infallible one.
- **A skin's joint list is not the set of bones that deform the mesh.** The
  buffalo carries `PoleTarget.L/R` and `PoleTargetBack.L/R` — IK pole targets,
  weighted to nothing, sitting *outside* the body by design, because a pole
  target belongs in front of the knee. Our own `rig-human` has the same shape
  for a different reason: `root` and two `thumb_04_leaf` tips, 63 of 66 bones
  deforming. `Document::non_deforming_joints` now reports them, because
  anything that asks "is this bone inside the mesh" or "should this be
  exported" gets the wrong answer for them.
- **`ChainKind::Control` is justified but not yet added.** The buffalo's pole
  targets are a chain kind our vocabulary cannot express, which is the bar the
  format sets. It is deliberately not added yet: the rule is that a kind needs a
  real template that cannot be described without it, and we cannot ship the
  buffalo as a template. Add it with the first control-bearing rig we can ship.
- The buffalo and rhino are both **unguligrade** and the hyena **digitigrade**,
  which is the posture split already recorded.
- **The giraffe's bones have no meaningful names.** Exported from its `.blend`
  via Blender (48 bones, 3 meshes, 2 actions — our reader agrees exactly), every
  bone is called `Bone.027` through `Bone.055`, and many are parented to nothing:
  31 chains for 48 bones. **This is the strongest argument yet for P3-4 matching
  on chain structure rather than names** — the legacy's 32-category name
  tokenizer has nothing to work with here, and a real user asset looks like this.

- [x] **P3-0** **Preserve an existing rig on import (O9)** — *done 2026-09-01, session 039* — *user requirement, 2026-08-30. Do this BEFORE the auto-rig workflow assumes it owns the skeleton.*
  - `crates/m2m-io/src/import.rs`: `inspect(bytes)` decides the format from the **contents**, never the extension, and reports `format`, `meshes`, `bones`, `skinned_meshes`, `clips`, `over_influence_limit`. Nothing strips anything — that is the whole point. `src-tauri`'s `import_model` command opens the picker and returns it; the inspector panel says *"This model is already rigged. Its skeleton, weights and clips are kept — re-rigging is yours to choose, not the default."*
  - **Two silent losses found by reading the code, not by a failing test.** (1) glTF holds influences in sets of four and the reader takes `read_joints(0)` only, so a `JOINTS_1` file lost influences 5-8 with nothing counting it — now `GlbReport::primitives_over_influence_limit`. (2) `SkinReport::vertices_over_influence_limit` is only ever written by `Skin::bind`, which needs the mesh geometry; the report `skin::parse_all` returns has never been through `bind`, so surfacing that field would have shipped **a metric that could not move**. Counted from the clusters instead, which is the same criterion without triangulating every mesh.
  - **A skeleton with no mesh is still a rig.** `rig-human.glb` is a skin naming 66 joints with no mesh node pointing at it; deciding "rigged" from the skinned-mesh count would have called it unrigged and then overwritten it. `already_rigged()` is `!bones.is_empty()`.
  - Acceptance **met and independently verified**: `examples/rebuild_rig.rs` on the reference rig, read by Blender at both ends — 65 bones, identical bone names, identical `bone_parents`, identical `bone_rest` for all 65 to 4 decimals, 2 meshes at 10514/14232 verts, `influences_per_vertex {1: 23117, 2: 1259, 3: 370}`, `weight_total` 24746.0, and the action at `curves=520,keys=76960,range=1.00-148.00` on both sides.
  - Mutation-tested **8 for 8**, two of them mutations of the fixtures rather than the code. One survivor was found and closed: `a_joint_shared_between_skins_is_one_bone` asserted 67 bones on a file with **one** skin and nine mesh nodes — the test's own premise was false, so the dedupe it claimed to cover was never exercised. Split into the honest test plus a real two-skin fixture built by duplicating the skin in the JSON chunk.
  - On import, detect whether the file already carries a skeleton and skin, and **keep them by default**: bone hierarchy, bone names, bind matrices, skin weights, and any animation. Re-rigging becomes an explicit action the user takes, not what happens if they do not read a warning.
  - **This inverts the legacy's default, it does not port it.** `ModelCleanupUtility.strip_out_all_unecessary_model_data` turns every `SkinnedMesh` into a plain `Mesh` and deletes `skinIndex`/`skinWeight`; only `strip_out_retargeting_model_data` preserves the hierarchy, and only the retarget flow calls it. `ModelAnalysisReport.ts:353` warns *"Mesh is already rigged. This workflow drops the existing skeleton"* — which is the behaviour to remove.
  - **No new read capability is needed**: `model::parse_all` already returns the hierarchy with world matrices, `skin::parse_all` the clusters and bind matrices, `animation::parse_all` the clips. What is missing is the decision and the UI affordance.
  - Acceptance: import an already-rigged FBX, export it again, and the Blender gate sees the same armature, bone count, bone names and action as the input — i.e. a rigged model survives a round trip through the app untouched. The reference rig (65 bones, 2 meshes, 1 action) is the fixture.
  - Related: O9's own weights must not be silently renormalised or truncated either — `MAX_INFLUENCES` is 4, so a file with 5+ influences per vertex loses some. That is already counted in `SkinReport::vertices_over_influence_limit`; surface it rather than dropping quietly.
- [x] **P3-1** Template definition format — **data-driven**, no Rust change to add a creature *(done 2026-08-31, sessions 027-028; all 9 templates annotated)*
  - `crates/m2m-rig/src/template.rs`: `ChainKind`, `Chain`, `Template`, `Skeleton`, `TemplateProblem`, and `Template::check`, which reports **every** disagreement in one pass rather than one per run.
  - **The check runs in both directions.** A chain naming a bone that does not exist is the obvious error; a bone *no chain claims* is the one that actually happens, when a skeleton gains a bone and the manifest is not updated. Contiguity is checked too: without it a manifest could claim all 66 bones exactly once and still describe a hierarchy that does not exist.
  - **All nine manifests** in `crates/m2m-rig/templates/*.json`, validated in CI against the real `.glb`: human 66, fox 49, bird 55, spider 56, snake 28, shark 33, horse 56, kaiju 58, dragon 99 — **500 bones, every one claimed exactly once**.
  - `tools/glb-chains.py` splits a skeleton into maximal parent→child chains and refuses to finish unless they cover every joint. It does the mechanical part; what a chain *is* stays a judgement. Reading a tree by eye is how the fox's ears were missed.
  - **Two kinds were forced into existence by real templates, which is the rule working.** `ChainKind::Accessory` (fox ears and belly; also horse ears and ribcage, dragon horns) and `Posture::Unguligrade` (the horse walks on hooves — a real third category, not a shade of digitigrade, because the ground contact is at the very end of the limb). Neither bird nor spider needed anything new, which is the useful part: they were chosen first *because* they looked hardest.
  - Judgement calls worth remembering: a snake's `tail01..tail20` is its **body axis**, so `Spine`, matching that kind's definition; a spider leg starts at its `legs_anchor_N` exactly as a human arm starts at its clavicle — the bone that attaches a limb belongs to the limb; a spider's posture is left **unset** because plantigrade, digitigrade and unguligrade all describe mammal feet and an arthropod is none of them.
  - Mutation-tested 10 for 10 across both sessions, most of them mutations of the **manifests** rather than the code. One survivor was found and closed: relabelling the shark's fins as legs passed everything, because the tests counted limbs but never asked what they were *for*.
- [x] **P3-2** Port the 9 existing templates into the new format *(done 2026-08-31 with P3-1 — all nine annotated and validated against their `.glb` in CI; 500 bones, each claimed exactly once)*
- [~] **P3-3** Landmark-based auto-fitting: place the skeleton from mesh proportions *(initial placement done 2026-09-01, session 029; per-chain refinement next)*
  - `crates/m2m-rig/src/fit.rs`: `Landmarks` (bounds, ground, symmetry plane, **medial_z**, `symmetry_error`), `RestPose`, `BodyAxis`, `fit_uniform`.
  - **The mesh cannot supply the body axis.** Measuring all nine base models: the human's widest extent is its **arm span** (1.933 across vs 1.830 tall), the bird's and dragon's are their **wingspans**, the spider's is its **leg spread**. Only the four quadrupeds and the fish are longest along the body. The template's own spine gives the answer — this is decision A7 earning its keep on the first real use.
  - `BodyAxis::{Upright,Horizontal}` decides **what the Z axis means**. Upright: Z is depth, so use the body's midline. Horizontal: Z is *length*, and the midline median slides the skeleton from nose to tail. Treating every creature as upright breaks the fox.
  - **`medial_z`, not the bounding-box centre.** Fitting the human rig onto `human-sophia.glb` with the box centre put the whole lower spine **behind** the body: her hair reaches z = -0.549 while the torso at pelvis height spans only [-0.157, +0.135], so the box centre sat 0.18 behind the body it described.
  - **Both sides of the depth alignment must measure the same thing.** Aligning the template's *bounding box* (which includes arms) to the mesh's midline was wrong by 0.027 on the base human — enough to push `human-female`'s spine_01 out of her chest. The template side is now its **spine**.
  - **The base rig/model pairs are a weak fixture**: they were authored together, so the fit is nearly the identity (scale 1.117 human, 1.014 fox; ground offset 0.0004). A mutation removing ground alignment entirely moved the human by 0.0004 and the inside-the-mesh check never noticed. The **variation** meshes are the real fixture — 0.99x to 2.30x the base height.
  - Acceptance: every spine joint inside the mesh for **7 of 8** bodies. `human-sintel`'s spine_03 sits 0.031 out (1.7% of her height) and carries a **stated budget** rather than a blanket tolerance chosen to swallow it — no single global scale matches every torso, and that is what per-chain fitting is for.
  - Mutation-tested 6 for 6: no ground alignment, scale from the box diagonal, align to the left edge, bbox instead of medial_z, every creature upright, and template box instead of template spine.
  - **Per-chain refinement DONE 2026-09-01 (session 030)**: `refine_spine` moves each spine joint onto the mesh's midline **at its own height**, which removes the sintel budget entirely — all 8 human bodies plus fox and horse now get every spine joint inside with **zero** allowance.
  - **Refinement is upright-only, and that is measured, not conservative.** Applying the same idea to a horizontal body makes it worse: a quadruped's backbone runs along the *top* of its torso and the slice median is dragged down by the legs. On the fox it moved the whole spine from y 1.08 to 0.74 — a fall of 20% of its height — and pushed two joints out of a body they were inside. A horizontal body keeps its uniform placement until there is a statistic justified by measurement.
  - **`ground_bone` is measured, not derived from posture.** The first version counted back from the end of the chain on the reasoning that a plantigrade sole contacts at the foot and an unguligrade hoof at the tip. The rest poses say the lowest bone is the **last** one in all three: `ball_leaf_l` 0.012 (human), `Back_Leg_Tip_L` 0.027 (fox), `back_leg_leaf_l` 0.011 (horse).
  - **What posture actually separates is ankle height**, and widely: 6% of template height for the plantigrade human, 21% for the digitigrade fox, 26% for the unguligrade horse. `ankle_height` returns `None` when a chain has no posture — a spider's leg and a bird's wing must not be handed a mammal's default.
  - **Limb fitting DONE 2026-09-01 (session 031)**: `fit_limb` / `fit_limbs` swing a limb chain about its attachment onto the limb the mesh actually has. Limb joints outside the mesh fell from **53 of 170 to 26 of 170** across seven bodies, with **no body made worse**.
  - **Grounding needed no work at all.** Measured before writing anything: leg tips already sat 0.7%–2.4% above the ground after the body fit, which is exactly where each rig's own tips sit above its own floor — `fit_uniform` maps the rig's floor onto the mesh's ground, so proportions carry. The work was all direction, not length.
  - **Legs ground regardless of posture.** Measured tip heights above each rig's floor: spider legs (no posture) 0.5–1.5%, bird (digitigrade) 2.4%, human (plantigrade) 0.7%. Arms sit at 87.8% and wings at 86.7%, so the rule keys on `role == Leg`, and posture governs proportions *within* a leg rather than whether it touches.
  - **The target rule took three attempts, and the first two broke bodies that had been perfect.** Furthest vertex in a 60° cone: fox 0→8 limb joints outside, leg tips lifted 52% of body height; bird tips 73%. Ranking by reach along the limb's own axis in a 32° cone fixed the tips but still cost fox 6 and horse 5. Only **skipping limbs already inside the mesh** made it monotone — the fox and horse rest poses already match their meshes, so re-aiming can only hurt them.
  - **A-pose/T-pose (P3-6) largely solved 2026-09-01 (session 032)**: `refine_limb_joints` pulls a limb joint that is outside the mesh onto the centroid of the mesh near it. **Limb joints outside fell from 26/170 to 2/170** — human 5→1, jay 6→0, bunny 8→0, sophia 3→1, bird 4→0, fox and horse untouched at 0. Five of seven bodies are now exactly zero.
  - **The legacy had no algorithm to port.** `RigModelVariations.ts` carries a hand-authored `expandArms` angle per model; only `bunny` sets one (−30°), and it poses the marketing page rather than rigging anything. Notably bunny was also the worst body measured here, so the number was real — it was just entered by a person.
  - **Same guard as `fit_limb`: only move a joint that is actually outside.** Pulling every intermediate joint to a local centroid helps the bodies that need it (bunny 8→4, sophia 3→1, bird 4→0) and hurts the ones that do not (fox 0→5, horse 0→6, human 5→6), because a centroid is the middle of the *nearby mesh*, not where a joint belongs when the template was already right.
  - **Endpoints are refined too**, which the first version excluded on the reasoning that an attachment belongs to the body and a tip was just aimed at the limb's end. Measured: on jay and bunny the endpoints were the *only* joints still outside (2 and 4), and including them takes both to zero.
  - Budgets now fox 0, horse 0, bird 0, jay 0, bunny 0, human 1, sophia 1.
- [~] **P3-4** Bone auto-mapping — **structural matcher done 2026-09-01 (session 033)**; the legacy's name-based path is *not* being ported 1:1, see below
  - `crates/m2m-rig/src/automap.rs`: `Skeleton`, `Skeleton::chains` (maximal parent→child runs), `Signature`, `match_skeletons`, `map_bones`. **Names are never consulted.**
  - **The legacy's limit, measured against its own resolver** rather than assumed: `BoneChainResolver.build_metadata` resolves **7 of 7** bones on a named humanoid and **0 of 17** on the same shape with bones called `Bone.000`. Its slots come from `resolve_slot(parse_bone_name(name), side)`; structure only propagates side and depth *after* a name has produced a slot. The user's giraffe is exactly this case — every bone `Bone.027`..`Bone.055`.
  - **Correction to an earlier claim in these notes**: the legacy is not purely name-based. `BoneAutoMapper` routes known rigs through Mixamo/Rigify tables and everything else through canonical slot resolution that *does* use chain position and side. The gap is narrower than stated: it cannot start without a name.
  - **Template chains are not maximal chains.** `human.json` heads its spine with `pelvis`, but `pelvis` has three children so topologically it *ends* the chain starting at `root`. Matching is therefore maximal-run to maximal-run; `map_bones` pairs bones within matched chains **proportionally**, so a 4-bone arm maps onto a 3-bone one end to end.
  - Verified: renaming every bone to `Bone.NNN` and re-matching recovers the **identity** on all seven rigs tested (human, fox, bird, horse, spider, shark, snake). Cross-rig, our human maps onto a Mixamo rig with **no bone crossing the midline**.
  - **Four mutations, four caught — but only after three fixtures were rebuilt.** Zeroing the side weight, zeroing the direction weight and index-pairing instead of proportional pairing all initially *survived*, because identity recovery is exact whatever the weights are and `.min(len-1)` clamps the chain ends. The fixtures that distinguish them: mapping a rig onto its **mirror image** (side), two chains alike but for **direction** at the same height and side, and asserting the **middle** bone of a 5-bone chain lands on the middle of a 3-bone one.
  - **Known-rig tables DONE 2026-09-01 (session 034)**: `crates/m2m-rig/known-rigs/{mixamo,rigify}.json` (65 and 52 bones), `KnownRig`, `normalised_bone_name`, `Strategy`, `map_bones_best`. Tables are **data**, not Rust literals, matching the crate's design rule. They are our own legacy's tables — a list of names is a fact about a format, and nothing here comes from Rigify itself.
  - **Measured why the tables earn their place**, rather than assuming: against the legacy's own Mixamo table, structural matching agrees on **41 of 65** bones and differs on 24 — and **every one of the 24 is a finger**. Index, middle, ring and pinky are four near-identical chains leaving the same hand and nothing in a chain's shape separates them. `structure_and_the_table_differ_only_on_fingers` pins that, so if structure ever learns to order fingers the test says so.
  - Names compare with punctuation and case stripped: the sample rig writes `mixamorig:Hips` where the legacy's table writes `mixamorigHips`. Detection needs a table to account for ≥50% of its own bones, so a rig sharing a few names by accident does not qualify.
  - `m2m-wrong-bone-names.glb` turns out to be a **Rigify-named** rig (`DEF-hips`, `DEF-spine.001`) and is now the fixture proving Rigify detection.
  - **Finger ordering SOLVED 2026-09-01 (session 035): structure now reproduces the Mixamo table exactly, 65 of 65.** `Signature` gained `parent_offset` — where on its parent a chain hangs.
  - **The scaling was the whole finding.** Expressing that offset as a fraction of *skeleton height* changed nothing (still 41/65, the finger errors merely shuffled from ring to pinky): a finger sits ~2% of a body height from the hand, so against terms weighted around 1.0 the difference is numerically invisible. Scaling it by the **parent bone's own length** made it decisive. A term can be the right idea and still do nothing if it is measured against the wrong yardstick.
  - `structure_reproduces_the_mixamo_table_exactly` replaces the old fingers-only assertion, which fired the moment it stopped being true — exactly what it was written for.
  - **Legacy baselines ported 2026-09-01 (session 036) — P3-4 COMPLETE.** What was ported, and what was deliberately not, is documented in a header comment in `crates/m2m-rig/tests/automap.rs`.
    - **Ported** (behaviour a user sees): Mixamo detected with and without its `mixamorig` prefix; other rigs (Unreal, DAZ, VRM) and an empty skeleton not mistaken for it; a prefix-stripped Mixamo rig still mapping through the table; a parent cycle survived; one source bone never assigned twice.
    - **Not ported** (machinery our design replaced): everything asserting a `BoneSlot`, which is parsed from a name and has no counterpart here; "numbers chains from the hierarchy, not from the digits in the name", which is true by construction for us; and the Unreal/DAZ/VRM *mapping* tests, because **those fixtures carry names and parents but no positions** — all the legacy needed, and not enough to run a structural matcher. Their name lists are still used for detection, where geometry is irrelevant.
    - **One deliberate divergence**: the legacy "leaves an unrecognisable rig unmapped rather than mapping it wrongly"; we map it structurally, which is the entire point.
  - `KnownRig::common_prefix` computes a table's shared prefix from the table itself (`mixamorig`, `def`) and accepts a name with it stripped, which is how the legacy tolerates exporters that drop it. Guarded so a stripped remainder under 3 characters is not offered — **proved a no-op on the shipped tables** (shortest remainders are `hips` and `toel`, 4 characters, none under 3), kept against a degenerate future table.
  - Prefix stripping widens what a table accepts, so `our_own_rig_is_not_taken_for_a_foreign_one` checks our own source rig still comes back unrecognised — Rigify's prefix is `def`, so its entries also match bare names like `spine`.
- [x] **P3-5** Port `Retargeter` logic (drop `Quat`/`Vec3`/`Transform` → `glam`) — *complete 2026-09-01 (sessions 037-038)*
  - `crates/m2m-rig/src/retarget.rs`: `RestRotations`, `RotationTrack`, `Clip`, `RetargetReport`, `retarget`. Uses `glam` throughout, per decision A4.
  - **The rest pose cannot be ignored, and that is measured.** The legacy's *default* path copies key times and values verbatim and merely renames the track (`AnimationRetargetService.retarget_animation_clip`), reserving a swing/twist path for humans. Between our human rig and a Mixamo rig, over the 65 bones their table pairs: rest orientations differ by a **median of 3.8°**, but `thigh_l`, `thigh_r`, `calf_l`, `calf_r` and `foot_r` differ by **~180°**. A verbatim copy puts the legs on backwards.
  - The algorithm: `motion = source_animated_world * inverse(source_rest_world)`; `target_world = motion * target_rest_world`; `target_local = inverse(target_parent_animated_world) * target_world`. Working in world space is what makes the 180° legs come out right — each rest pose cancels against its own side.
  - **Retargeting preserves motion relative to rest, not absolute orientation.** My first acceptance test asserted the two rigs' limbs end up pointing the same way in the world, which is false by construction when their rest poses are 180° apart. The right property is that the *change* in world orientation between two instants matches.
  - **A fixture found a real bug**: an undriven bone was pinned to its rest *world* rotation, so it fought its parent — an unmapped hand would hang in the air while the arm swung. It now keeps its rest *local* rotation and follows the parent. Every earlier fixture had an unanimated parent, so nothing could see it.
  - Mutation-tested 5 for 5, but **three survived until fixtures were rebuilt**: dropping the source rest compensation is invisible when the source's rest is identity, and skipping the parent division is invisible when the parent is never animated.
  - **Translation, root motion and the end-to-end path DONE 2026-09-01 (session 038) — P3-5 COMPLETE.** `RestTranslations`, `TranslationTrack`, `height_scale`, `retarget_translations`, and `crates/m2m-rig/examples/retarget_glb.rs`.
  - **Bone translations are structural, not animation — measured.** Across the 87 clips in `human-base-animations.glb`, of **5,809** translation channels only **94 move**, and they belong to exactly two bones: `pelvis` (80 clips) and `root` (14). Everything else is a constant equal to the rest offset, written by an exporter that emits full TRS regardless. Copying them across would rebuild the target with the *source's* bone lengths.
  - So translation uses the same rule as rotation, with no special case for the root: `target_rest + (source_value - source_rest) * height_scale`. A bone that never moves has a zero offset and lands exactly on the target's rest, preserving the target's proportions; the root's motion is scaled so a taller character strides further instead of shuffling.
  - **No `root_correction_x_degrees` is needed.** Our rig's `root` carries a rest world rotation of (0.707, −0.707, 0, 0) — a −90° X, the Z-up→Y-up correction — and Mixamo's rig has no root at all. That difference is exactly what the legacy compensated by hand, and it is subsumed by rest-pose compensation, which its default verbatim-copy path did not do.
  - **Verified end to end by two independent readers.** Retargeting all 87 clips onto the Mixamo rig and writing the result: **Blender** reports 65 bones, 87 actions, `Chest_Open` at frames **0.00–33.00** — the same range as the source; **assimp** reports 87 animations and 5,655 channels (65 bones × 87). Curves read 455 rather than 660 because we emit rotation and translation and no scale channels.
  - Known cost: every track is resampled onto the union of the clip's key times, so key counts rise (9,230 against the source's 5,128 for `Chest_Open`). Correct, but worth revisiting if file size matters.
- [~] **P3-3b** Spine refinement for **horizontal** bodies *(2026-09-01, session 042 — improved, not finished)*
  - `refine_spine` was upright-only. Measured for the first time across all nine creatures through the new one-call pipeline: **16 of 68 spine joints landed outside the mesh**, every one of them on the **snake (6) and shark (10)** — the two creatures whose spine *is* their whole body, long and tapering, where a uniform scale diverges most. Every other creature was already at zero.
  - The obvious mirror of the upright case — slice along Z, take the median Y — **is refuted**: it scored better overall (17 vs 16 counted wrongly, shark 10→4, snake 6→5) while putting **fox, horse, bird and dragon** outside bodies they had been inside. Recorded because it looks right and is not.
  - What shipped adds the guard `fit_limb` already states in its own docs: *a joint the body already contains is already placed*. Result **16 → 11**, and **no creature regressed** — the seven zeros are now an asserted invariant. Shark 10→5, snake 6→6.
  - **P3-3b DONE 2026-09-01 (session 053)**: `snap_spine_into_mesh` nudges a barely-outside spine joint onto the body **in its own Z cross-section** (keeping its position along the body, correcting only X/Y) using the `VoxelGrid` the limb fitter already builds — the real containment `refine_spine`'s slice y-range could not do. Snake **6→5**, shark **5→3**, seven zeros untouched (it only moves voxel-outside joints). The remaining are **past the end of the mesh tail** — the template tail carries more bones than the shorter tapering mesh reaches — and are correctly left rather than bunched. Mutations 4/6 (two no-ops: an equivalent `map_or` rewrite, and the upright-axis guard no upright fixture exercises).
  - ~~Remaining 11~~, all on snake and shark tail tips. The containment test is a slice's y-range, and a point can sit between a cross-section's lowest and highest vertex while still being outside the body — which is exactly why snake's `tail18` is not fixed. A real containment query (the `VoxelGrid` the limb fitter already builds) is the obvious next move; `refine_spine` does not currently take one.
- [x] **P3-3c** One-call fitting pipeline and shipped templates *(done 2026-09-01, session 042)*
  - `m2m_rig::fit::fit_template(template, rest, mesh, resolution)` runs uniform placement, spine refinement, limb swing and joint pull in one call. Every caller used to chain them by hand **and they did not agree** — `examples/fit_report.rs` stops after `fit_uniform` and never voxelises, so the numbers it printed were never the numbers the tests asserted. `the_one_call_pipeline_is_the_hand_chained_one` pins the composition.
  - `m2m_rig::template::all()` returns the nine manifests, **embedded** in the binary, so nothing has to be found on disk or packaged beside the app. Globbed by `build.rs` rather than listed, because `lib.rs`'s design rule says adding a creature must never require a change in this crate — a hand-written list of `include_str!` would break that on the first new species. All nine rig `.glb` files total 158 KB, so embedding the skeletons too is cheap when the app needs them.
- [~] **P3-6a** **Choose Skeleton step, end to end** *(done 2026-09-01, session 043)*
  - `src-tauri/src/rig.rs` is the bridge: `m2m-rig` deliberately does **not** depend on `m2m-io` — rigging maths has no business knowing file formats — so the `glb::Document -> RestPose/Mesh` projection lives in the adapter, where it belongs and is unit-tested (6 tests, the first `src-tauri` has ever had).
  - Commands `skeleton_templates()` and `fit_skeleton(template, path)`. The fit runs on `spawn_blocking`: voxelising at 128 cubed is real work and would otherwise stall the window. The result travels as **JSON**, not over the bulk channel — a skeleton is a few hundred bones, and `architecture.md` §4 draws its line at bulk geometry, not at everything numeric.
  - The nine rig `.glb` files are **embedded** (158 KB total), globbed by `src-tauri/build.rs` exactly as `m2m-rig` globs its manifests, so adding a creature stays a matter of adding files. `available` is reported per template, because a manifest naming a rig that is not bundled would otherwise fail only when a user picked that creature.
  - `glb::Document::world_transforms()` moved into `m2m-io`, where it should always have been: it had been written **three times** across `m2m-rig`'s tests and examples. It resolves each chain on demand and memoises, since glTF does not require a node to follow its parent, and marks before recursing so a parent cycle in a hostile file resolves to the identity instead of overflowing the stack.
  - The viewport draws a fitted skeleton as line segments (`skeletonSegments`, pure and tested) — three's `SkeletonHelper` cannot help here, because a template skeleton arrives as bare positions and parent indices with no scene graph to attach to. Drawn with `depthTest: false`: a skeleton the body hides is a skeleton nobody can check.
  - **The step rail advances for the first time**, gated on `furthestStep` — real progress, not where the user is standing, so going back does not re-lock what was earned.
  - **A survivor proved a no-op and the code got smaller.** The parent lookup was a nearest-joint-ancestor walk; a mutation replacing it with the direct parent survived. Measured across all nine rigs: the two rules agree on **every joint of every rig**, and each rig has exactly one root. The walk was untested defensive code, so it was removed and replaced by `every_rig_hangs_its_bones_directly_off_each_other`, which fails loudly if a future asset breaks the assumption.
- [x] **P3-3d** DONE (session 063, 84fd3a7): Move the nine rig `.glb` files out of `legacy/static/rigs/` — they are product assets living in the legacy tree, which reads as a mistake to anyone new. **DEFERRED (session 062): more cross-cutting than the todo assumed.** The rigs are shared: consumed by `src-tauri/build.rs` (embeds them), `m2m-rig`/`m2m-io`/`m2m-bridge` tests (4 files), AND the legacy benches (`legacy/bench/solver-baseline.ts`, `dump-fixtures.ts` load `static/rigs/`). Moving them touches the build system + the FROZEN legacy tree (kept as a pure git-mv A/B reference) — app-bundle risk + a legacy-tree edit we avoid. `build.rs:19` itself calls it "churn for another day". Needs deliberate human sequencing (pick a neutral home, update legacy + new refs together), not a 60s autonomous sprint.
- [ ] **P3-6** Frontend shell: step rail, inspector, guidance strip per `design.md` §5
- [~] **P3-7** Port viewport: Three.js scene, `CustomTransformControls`, `CustomSkeletonHelper` — *scene, camera and skeleton overlay done 2026-09-01, session 041; transform gizmo deferred*
  - `app/src/viewport/model.ts` holds everything decidable **without a GPU** — `parseModel` (GLTFLoader over the bulk-channel buffer) and `frameBounds`/`applyFraming` — so the part that can be tested is separated from the part that cannot. `scene.ts` is the glue that needs a screen and is deliberately thin.
  - **Rendering is event-driven from the start**: no standing `requestAnimationFrame`, one frame per change, orbit damping off because damping needs frames after the input stops. That is P4-5's requirement, and it is far cheaper to build in than to retrofit — an always-on loop lets state changes go unannounced and everything quietly comes to depend on it.
  - **The frontend can verify the wire format.** three's `GLTFLoader` runs under Node once `self` is aliased (it reaches for it only on the texture path), so `app/tests/` parses a real `.glb` with the very loader the viewport uses: 66 bones, 1 skinned mesh, 87 clips, 1.83 m tall. `npx vitest run` is now a CI step; `vitest.config.ts` includes only `app/tests/**` so `legacy/`'s own suite is untouched.
  - **A floor was removed, not added.** `near = max(distance/1000, 0.001)` clamped exactly the small subjects it looked like it protected — a 20 cm model wants a near of half a millimetre — and made the depth range non-proportional. Depth precision depends on the near/far *ratio*, which is constant. Found because the test asked the range to MOVE rather than to bracket the subject: a fixed near/far passes any "near < distance < far" assertion.
  - Deferred: **`CustomTransformControls`** belongs with the Fit Skeleton step, not with displaying a model — filed as P3-7b. The stock `SkeletonHelper` is used rather than a custom one; three ships it and nothing yet needs thicker bones.
  - Known: models converted from FBX carry no normals, which glTF answers with flat shading. Real visual difference, not a load failure. Bundle grew 11 KB to 642 KB (three.js), against a 40 MB budget.
  - Mutation-tested **9 for 9**, one of them a data mutation swapping the rigged fixture for the plain mesh.
- [x] **P3-7b** DONE (session 063, c3bf233): Transform gizmo for editing bone placement — take with the Fit Skeleton step, where it has a caller
- [~] **P3-8** Binary IPC layer with progress events (`architecture.md` §4) — *bulk channel done 2026-09-01, session 040; progress events deferred*
  - **glTF is the wire format.** §4 describes bulk data as bytes with a small JSON header, which is exactly what a `.glb` is — so neither side needs a private encoding and the frontend can hand the payload to a loader that already exists. `load_model` returns `tauri::ipc::Response`, whose body is `InvokeResponseBody::Raw` (verified in `tauri-2.11.5/src/ipc/mod.rs:99-104,190-202`, not quoted from memory).
  - `m2m_io::import::load` brings **either** format to `glb::Document`; `inspect` and `load` share one private `read_any`, so the format sniffing exists once.
  - **`crates/m2m-io/src/convert.rs` — FBX into the glTF document model**, which had to exist first: node hierarchy with local transforms, triangulated meshes, skins with joint lists and inverse bind matrices, and the unit scale. **Animation is not carried yet** — the reader and `glb::Clip` both already have it, so it is work rather than a question, and the viewport needs geometry before playback. Normals and UVs have nowhere to go: `glb::Primitive` has no field for them.
  - **Units are read, not assumed.** `Scene::unit_scale` now carries `GlobalSettings/UnitScaleFactor` beside `time_mode`, for the same reason: dropping it silently rescales space, and a converter that assumes metres makes a 180-metre character. The factor goes on the **root** nodes only, so everything inherits it once; inverse bind matrices are deliberately left unscaled, because a joint's world becomes `S · L` while its matrix stays `L⁻¹ · M`, giving `S · M`. Scaling both would cancel out.
  - **Progress events deliberately not built.** There is no long-running job to report on yet — `bind_skin` is not wired. Building the event plumbing now would be guessing at its shape.
  - **Known cost, measured:** FBX geometry is unshared per corner, so the reference rig crosses as **62,520 and 84,816 vertices instead of 10,514 and 14,232** (5.9 MB of `.glb`). Welding on position+joints+weights would be lossless here, since normals and UVs are not carried. Worth doing before a 200k-vertex model does 50 MB.
  - Verified by two independent readers: **Blender** — 65 bones, names identical to the FBX, 2 meshes, 1 armature, and the weight distribution matching to 0.02% (93.42% single-influence in the FBX vs 93.39% in the GLB); **assimp** — meshes and faces (49,112) exact. Its bone count differs (129 vs 95) and **is not explained**: two hypotheses were measured and both refuted (115 deforming joints, then 89). Recorded rather than rationalised.
- [ ] **P3-8b** Progress events for long jobs (`invoke → job_id`, `m2m://progress`, `m2m://done`) — take this with the first operation slow enough to need it, which is weight binding
- [x] **P3-8c** **Weld the per-corner expansion in the FBX converter** *(done 2026-09-01, session 052)*
  - `fbx_to_gltf` welds by **source vertex**, not by position. `Mesh::weld_map` welds by position, which would wrongly merge two coincident FBX source vertices that carry different skin weights; the source key is provably lossless because `geometry::parse` only duplicated per-corner normals and UVs (which we do not carry), and `Skin::bind` fills every corner of a source from the same `per_source` entry, so all corners of a source are identical in position and weights.
  - **Measured, directly serving the RAM/CPU priority**: the reference rig's two meshes drop from **62,520 → 10,514** and **84,816 → 14,232** vertices, exactly the source counts Blender reports for the original FBX. The bulk-channel payload falls from **5.9 MB to 1.5 MB** (3.9x). Triangle counts unchanged (20,840 / 28,272).
  - **Blender confirms lossless**: the welded GLB reads `mesh_vertices [10514, 14232]`, `weight_total 24746.0`, `influences_per_vertex {1:23117, 2:1259, 3:370}` — identical to the source FBX's own Blender report. The converted GLB now matches the original vertex-for-vertex.
  - Two tests: the count/normalisation invariant, and a per-corner losslessness check that needs no knowledge of the new numbering — corner `i` welds to `welded.indices[i]`, whose position and first influence must equal the corner's. Mutations 5/5, including "the weld runs at all".
- [~] **P3-6b** **Bind Weights step** *(done 2026-09-01, session 044 — solve and report; the paint overlay is P3-9)*
  - `rig::bind(model, skeleton, falloff)` runs the whole solver: mesh, voxel grid, geodesic field per bone, `assign_weights`. Measured on the reference body — **7,399 vertices, 66 bones, fit 21 ms and the whole bind 56 ms**, which is why there is still no progress event (P3-8b stays deferred on evidence rather than on hope).
  - **Bone segments run from a joint towards its FIRST CHILD.** The tempting alternative — parent's joint down to this one — is off by one and would weight the shoulder to the upper arm. A branching joint like the hips takes its first child in joint order; that is a judgement call, and the alternatives (a point at the joint, or the children's average) lose the torso's reach. Nothing in the codebase established this: the `m2m-core` fixtures read pre-baked segments from a binary, so the convention had to be chosen and is now pinned by a direct test.
  - The **root and every leaf are excluded** from weight, which is `BoneMask`'s documented legacy invariant. On the human rig that is 1 + 13 = 14 of 66.
  - **Measured difference in character worth an A/B**: the geodesic solver gives **96%** of vertices the full four influences ([254, 3, 38, 7104]), where the reference Mixamo export gives **93%** of its vertices exactly one ({1: 23117, 2: 1259, 3: 370}). Whether ours is too smooth is exactly what `memory/test.md` §9's legacy A/B is for. Recorded, not judged.
  - **The geodesic field spreads through the VOXEL GRID, not mesh topology.** Nine separate meshes sitting against each other report **zero** fallbacks; only geometry with real empty space around it takes the straight-line path. Getting a fixture for it meant building one — two unit cubes 10 apart, written with `glb::write` — and it reports exactly the far cube's 8 corners.
  - **A survivor kept and documented rather than covered.** `unweighted_vertices` can never be non-zero: `assign_weights` always writes at least one influence. A mutation folding it into the histogram survives **by construction**, not for want of a fixture. It stays as a canary on another crate's invariant, and the doc comment says so.
  - Weights themselves stay on the Rust side — `vertices x 4` indices and floats, about 600 KB — and belong on the bulk channel with the geometry when something draws them.
- [~] **P3-6c** **Export step: the app produces a rigged file** *(done 2026-09-01, session 045 — `.glb`; FBX export is P3-6d)*
  - `rig::export_glb(model, skeleton, falloff)` assembles mesh + skeleton + weights into a `glb::Document` and writes it. Command `export_model` opens a save dialog off the main thread.
  - **Weights are recomputed, not carried.** `bind` and `export_glb` share a private `solve`. Sending ~600 KB out to the webview and back would cost more than a 56 ms re-solve, and would leave a cache to invalidate every time a bone moved.
  - **Bone rotations are identity, deliberately.** A fitted skeleton is a set of joint *positions*; inventing orientations would be making up data the fitter never produced. Positions are what deform the mesh, so the export is complete for skinning even though bone roll is not recovered.
  - **Verified by two independent readers.** Blender: 66 bones, one root (`root`), 1 armature, 7,399 vertices, 0 loose, 66 vertex groups, `weight_total` **7,399.0** — every vertex sums to exactly 1.0 — and `influences_per_vertex {1:254, 2:3, 3:38, 4:7104}`, **identical to the BindReport histogram**. assimp: meshes, faces (13,757), animations and primitive types all match; its node and bone counts differ because the source was unrigged and we added a rig, and its glTF bone count (22) is the same **unexplained** counting difference recorded in P3-8.
  - **Two mutation survivors were real test gaps, both about the mesh-to-skin link.** Dropping `skin: Some(0)` from the mesh node, or the primitive's `node`, left every count I asserted looking correct while the file would import **unweighted** — the exact failure `glb::Node`'s own doc comment warns about. Now asserted: exactly one node carries the skin, and the primitive hangs off that node. 6 of 6 caught after.
- [x] **P3-6d** **FBX export** *(done 2026-09-01, session 046)*
  - `rig::export_fbx` assembles `build::{Mesh, Bone, Cluster, Skin, Scene}` and encodes. The export step offers both formats.
  - **Units were the trap.** `fbx::build` hardcodes `UnitScaleFactor` 1.0, which declares **centimetres**, while our meshes are metres. Positions and bone offsets are multiplied by 100 on the way out; without it the file imports as a 1.8 cm character and no count of bones or vertices shows it.
  - **Cluster inversion**: weights arrive per vertex with four slots, FBX wants them per bone as parallel index/weight lists. A bone influencing nothing gets **no cluster**, not an empty one.
  - **`build` panics on a parent that appears after its child**, and a `FittedSkeleton` arrives from the frontend, so the order is checked and refused as `RigError::BadBoneOrder`. Measured first: all nine shipped rigs are already parents-first (0 violations of 500 joints).
  - **THE 22-vs-66 MYSTERY IS SOLVED.** Recorded as unexplained in P3-8 and P3-6c: assimp reported 22 bones where Blender's glTF import reported 66. Blender's **FBX** import of our own export reports `vertex_groups` **22** — the cluster count. So assimp and Blender-FBX count bones that *carry weight*; Blender-glTF counts *declared joints*. Three readers agree on 22. Two earlier hypotheses had been measured and refuted; this one is confirmed by a third reader rather than argued.
  - **Measured and worth an A/B**: only **22 of 52** weightable bones receive any influence on `model-human.glb` (7,399 vertices). At that resolution the finger bones lose to the hand in every vertex's top four. Ties to the open question about the solver's smoothness (`memory/test.md` §9).
  - **assimp reports the GLB and FBX exports as IDENTICAL on every field** — nodes 68, meshes 1, faces 13,757, bones 22, animations 0, vertices 7,011 vs 7,011. Blender agrees across both: 66 bones, 1 armature, root `root`, 7,399 vertices, `weight_total` 7,399.0, influences `{1:254, 2:3, 3:38, 4:7104}` — the same histogram the BindReport computes by a different path.
  - **Two survivors were the same class of gap as last session** — counts asserted, connections not. Neither an unscaled mesh (skeleton 100x the body) nor an identity `transform_link` (every vertex bound as if its bone sat at the origin, so the mesh tears apart when posed) failed anything, and **neither Blender's nor assimp's report would have said a word**. Now asserted directly: mesh and skeleton agree in scale, and every cluster's `transform_link` equals its bone's world transform. 6 of 6 caught after.
- [x] **P3-6e** **Exported rigs keep the template's rest rotations** *(done 2026-09-01, session 047 — a correction to P3-6c and P3-6d)*
  - **What was wrong.** Both exports wrote **identity** bone rotations, justified in a doc comment as *"a fitted skeleton is a set of joint positions, and inventing orientations would be making up data the fitter never produced"*. The fitter does not produce them — **the template does**, and they were in hand and dropped. Measured: **all 66 bones of `rig-human.glb` carry a non-identity local rotation, up to 179.94 degrees.** The cost is bone roll (artists notice) and, worse, every clip authored against the template lands misaligned, because the clip's rest pose and the exported rig's no longer agree. Found while thinking about what retargeting onto a fitted skeleton would even mean — i.e. by the *next* feature's requirements, not by any failing test.
  - `FittedSkeleton` gains `rotations`, taken from the template rig and paired **by bone name** (`Fitted` keeps the rest pose's order today, but nothing in either type says it must).
  - Both exports now compose them: **a child's local translation is expressed in its parent's rotated frame**, and the inverse bind matrix includes the bone's world rotation. `check_bone_order` is shared and now guards the GLB path too — the forward pass that composes world rotations is only correct parents-first.
  - **The Euler trap.** FBX stores `Lcl Rotation` as Euler **degrees**, and a model with no `RotationOrder` reads back through `EulerOrder::Zyx`, which `fbx::transform::euler_matrix` composes as `Rz * Ry * Rx` — that is **extrinsic** XYZ, glam's `XYZEx`, not the intrinsic `XYZ` of the same name. Using the intrinsic one put every cluster **82 cm** from its bone. Pinned by `an_fbx_euler_round_trip_recovers_the_quaternion`.
  - One tolerance was **raised with a reason, not slackened**: the FBX bind-pose check went 1e-6 → 1e-3 file units because `transform_link` is no longer a bare translation — an f32 quaternion is now decomposed to Euler degrees and recomposed. Measured residual **5.0e-5 cm**, half a micrometre on a 180 cm character.
  - **A mutation survivor was the defect itself.** Replacing the carried rotations with identity passed *every* bind-pose assertion, because those are self-consistent about whatever rotations they are given. Only comparing the exported rotations against the template catches it. 6 of 6 after.
  - Re-verified: Blender reads both formats — 66 bones, 1 armature, 7,399 vertices, `weight_total` 7,399.0, influences `{1:254, 2:3, 3:38, 4:7104}`; assimp reports the two exports identical on every field.
- [x] **P3-6f** **Animate — end to end** *(done 2026-09-01, sessions 048 and 049)*
  - The libraries are **bundled resources**, not embedded: `tauri.conf.json` maps `../legacy/static/animations/*.glb` to `animations/`, resolved with `BaseDirectory::Resource` and falling back to the repository so `tauri dev` works from a checkout. **Measured before deciding**: CI's own size step reported the bundle at **8,028 KB against a 40,960 KB budget**, so all ~16 MB fit with room to spare — the "ship human only" contingency was unnecessary.
  - `animation_clips(template)` lists them; the Animate step offers them; `export_model` carries the chosen one. **Every one of the nine templates has a library** (`every_shipped_template_has_a_library_the_bundle_will_carry`), the human's under its second candidate name (`human-base-animations.glb`).
  - **FBX with a clip is refused, not silently dropped** — `fbx::build::Clip` wants Euler curves in ticks where retargeting produces quaternions, and `build.rs` calls that conversion lossy and ambiguous. The button disables itself and says why. Filed as **P3-6g**.
  - **NOT done**: viewport playback (three's `AnimationMixer`). The clip is chosen and exported, not previewed.
- [x] **P3-6g** **FBX clip export** *(done 2026-09-01, session 050)*
  - `export_fbx` now takes the same `animation: Option<(&[u8], &str)>` as `export_glb` and writes the retargeted clip as FBX Euler curves in ticks.
  - **`build.rs` calls the quaternion-to-Euler conversion "lossy and ambiguous", and for a ROUND TRIP it is right** — which is why `examples/rebuild_rig.rs` passes the original curves through untouched. A *retargeted* clip has no original curves, so the conversion is unavoidable and the ambiguity had to be handled rather than dodged.
  - **The hazard is continuity, not accuracy.** Every rotation has two Euler triples — `(x, y, z)` and `(x+180, 180-y, z+180)` — and each component is free modulo 360. Taking the canonical triple per key independently reconstructs every key *perfectly* and can still jump a full turn between neighbours, which an importer interpolates as a spin. Each key is now chosen nearest its predecessor: both triples unwrapped to within half a turn of the last, closer one wins.
  - **Verified in Blender**: action `Armature|Chest_Open|Layer0`, `curves=660`, `keys=22440`, **`range=1.00-42.25`** — 41.25 frames at the declared 30 fps (TimeMode 6) is exactly the source's 1.375 s. The GLB export of the same clip reads `0.00-33.00` because Blender's glTF import uses the scene's 24 fps; both are 1.375 s.
  - **Two mutation survivors were fixture weaknesses, not code gaps.** A pure Y spin cannot tell intrinsic XYZ from extrinsic — for a single-axis rotation the two orders agree — so the fixture now rotates about a tilted axis. And nothing checked translation magnitude, so metres-instead-of-centimetres passed every count and time-axis check while the character barely moved; the clip's vertical travel is now asserted. 5 of 5 after.
  - Tolerance raised with a reason: reconstruction is within **0.056 degrees**, and that is **gimbal lock** — the fixture passes exactly through y = 90 where an XYZ Euler's outer angles are ill-conditioned. Worst case by construction, far below anything visible.
- [x] **P3-6h** **Viewport playback of the selected clip** *(done 2026-09-01, session 051)*
  - `preview_animation` command returns the rigged+animated model as a `.glb` over the **bulk channel** — the very bytes a `.glb` export writes, via the same `export_glb` path, so preview and export cannot drift.
  - `app/src/viewport/model.ts` gains `parseAnimated` and `findClip` (GPU-free, tested); `scene.ts` gains `playAnimated`/`stop` driving a three `AnimationMixer`. **The frame loop runs ONLY while a clip plays** — the event-driven renderer keeps an idle viewport at 0 cost, and `stop`/`dispose` end the loop. The imported mesh is hidden during playback (they occupy the same space) and restored on stop.
  - **`findClip` tolerates importer-decorated names**: our exporter names the clip exactly, but an FBX round trip yields `Armature|Chest_Open|Layer0`. Exact match first, contains second — and *exact wins over a longer name that merely contains it* (`Chest_Open` beats `Chest_Open_Slow`), which a mutation exposed the fixtures did not force until a test was added.
  - **Verified end to end in Node** (pixels are untestable here, the data path is not): the preview `.glb` drives an `AnimationMixer`, and a hand bone (`index_01_r`) moves **0.47 m** between t=0 and mid-clip — real motion, not a still. 87 clips parse, `Chest_Open` at 1.375 s.
  - 17 frontend tests (was 12), mutations 3 of 3 after adding the exact-wins test.

**The six-step flow is now complete end to end: import → choose skeleton → fit → bind → animate (pick + preview) → export (.glb or .fbx, mesh + skeleton + weights + clip).**
- [x] **P2-11** **Fuzz crash in the GLB read path: an unparseable JSON chunk was waved through** *(fixed 2026-09-01, session 049)*
  - CI went **red** on `bbb6024` — a genuine crash, not flakiness. `gltf-json-1.4.1/src/mesh.rs:151` indexes `root.accessors` **unchecked** in its validation hook: "index out of bounds: the len is 0 but the index is 0".
  - **The bug was in a guard's escape hatch.** `check_indices` returned `Ok(())` when `serde_json` could not read the JSON chunk, reasoning that "malformed JSON is the `gltf` crate's error to report". That rests on the two parsers agreeing about what is malformed — and they do not. The crashing chunk holds **invalid UTF-8** (byte `0xd6`): `serde_json` rejects it, `gltf` accepts it, so **every index check was skipped** and the panic happened inside a crate we do not control.
  - Now refused as `GlbError::UnreadableJson`: a chunk we cannot parse is a chunk we cannot validate, and glTF requires it to be valid UTF-8 JSON. Pinned by a minimal reproducer, so CI catches a regression without waiting for the fuzzer to rediscover it.
- [x] **P3-9** **Weight-paint visualisation + auto-flagging of bad regions** *(done 2026-09-01, session 054)*
  - `rig::overlay_glb` bakes a per-vertex `COLOR_0` into the bound model: each vertex the **hue of its dominant bone** (golden-angle walk of the wheel, so neighbours never share a hue and no palette is maintained), and each **fallback vertex** — one the geodesic field could not reach, guessed by straight line — flagged flat red. That is the "bad regions" the design asked to auto-flag: `bind` counts them, this shows *where*.
  - `glb::Primitive` gained a `colors: Vec<f32>` field and `glb::write` emits `COLOR_0` (Vec4 F32) when present — a general glTF vertex-colour capability, not overlay-specific. The reader does not surface it (not part of the rig pipeline), so the overlay test asserts the `COLOR_0` accessor in the JSON chunk directly.
  - Command `weight_overlay(path, skeleton, falloff) -> Response` (bulk channel), shares `solve` with the exports so the overlay always shows the weights the export would write. Viewport `showOverlay` draws it with a `MeshBasicMaterial{vertexColors:true}` (unlit, so the flag reads as a flag).
  - **Verified in Node**: three's GLTFLoader reads all 7,399 vertices' colours, **0 exact fallback-flags** (matching `bind`'s `fallback_vertices: 0` for the human), 19 distinct dominant-bone hues.
  - Mutations 4/4 Rust (flag, dominant-bone-is-max, golden-angle hue, colours-baked) + 1/1 TS (`hasVertexColors`).
- [ ] **P3-10** Creature-specific guidance content for all templates (`design.md` §7)
- [ ] **P3-11** Undo/redo across every step
- [ ] **P3-12** Accessibility pass — keyboard, contrast, reduced-motion, ARIA (`design.md` §10)
- [ ] **P3-13** New creature templates from R-5 assets

### P3-P — Pose-agnostic humans: A-pose **and** T-pose *(added 2026-08-29, user requirement)*

Today A-pose is a workaround, not support: `human-a-pose.glb` is a test file
special-cased into the model list (`legacy/src/lib/DOMUtilities.ts:445-450`),
`ArmWeightCorrector` + the arm-plane-offset slider exist *because* A-pose arms
hang near the ribcage, and `ArmExtensionControl` is a manual percentage nudge
between the two. Four distinct problems:

- [ ] **P3-P1** *Weights* — A-pose arms running close to the ribcage. **Already solved by the geodesic solver** (P1-4/P1-5): the arm-to-ribcage confusion is precisely the Euclidean failure mode, since geodesically the ribcage is a long way up the arm and down the torso. No new algorithm; add `human-a-pose.glb` to the P1-7 invariant suite and the P1-8 A/B so the claim is proven, not assumed.
  - **Blocked on headless skeleton fitting.** Attempted 2026-08-29 and backed out: the benchmark applies the template rig with no fitting step, but `rig-human.glb` is a T-pose rig (`hand_l` at world x=0.75) while `human-a-pose.glb` has arms down (mesh x spans ±0.62 vs the T-pose mesh's ±0.97). Benchmarking that pair measures a rig/mesh mismatch, not A-pose rigging.
  - **The aggregate metrics cannot detect this defect anyway** — measured: A-pose and T-pose score 85% vs 87% single-influence and 1.151 vs 1.132 mean influences, essentially identical. The defect is about *which* bone claims a vertex, not how many influences it has. P1-8 needs a targeted metric (torso vertices whose dominant bone is in the arm chain, measured against the **fitted** skeleton). A first attempt at this was removed for measuring the wrong thing.
- [ ] **P3-P2** *Pose detection* — classify an incoming humanoid mesh as A-pose, T-pose, or something else, from the shoulder→wrist vector angle against the horizontal. Report it in the UI; never silently guess.
- [ ] **P3-P3** *Skeleton fitting for either pose* — the human template must land correctly on both. Either ship two rest variants, or (better) fit the template then rotate the arm chain to match the detected pose, so one template covers the continuum including the in-between poses artists actually model.
- [ ] **P3-P4** *Retargeting across a pose mismatch* — **the real technical meat.** A clip authored on a T-pose rig applied to an A-pose bind needs the rest-pose delta applied per bone, or the arms sit wrong for the whole clip. `RetargetUtils.capture_bone_rest_transforms` already recovers rest transforms from `boneInverses`, so the machinery exists; the retargeter must compose `source_rest⁻¹ · target_rest` per bone instead of assuming a shared rest pose. This is what makes Mixamo clips work on an A-pose character.
- [ ] **P3-P5** Re-evaluate `ArmExtensionControl` once P3-P4 lands — a manual arm-raise percentage is a symptom of the missing rest-pose delta, and may be deletable. Keep it as an artist override only if it still earns its place.
- [ ] **P3-P6** Extend the same treatment to non-human templates where a natural rest-pose ambiguity exists (birds: wings folded vs spread; quadrupeds: standing vs splayed).

## P4 — Bridge, performance, release

- [~] **P4-1** **`m2m-bridge` headless Blender inspection** *(done 2026-09-01, session 055 — the headless half; live add-on round-trip deferred)*
  - `m2m_bridge::inspect(bytes, ext, blender) -> BlenderReport` writes the bytes to a temp file, spawns `Blender --background --factory-startup --python <embedded script> -- <model> <report.json>`, and parses the typed report. This packages the `tools/blender-fbx-import-check.py` round trip — the only INDEPENDENT reader in the project — into a tested crate, the foundation the visual-regression harness (P4-3) needs.
  - **Reads the report from a FILE, never stdout** — Blender writes unterminated progress to stdout, which once made a gate fail with a JSON `SyntaxError` instead of its assertion. The script is embedded via `include_str!` from `tools/` so there is one source, no drift. Temp files are cleaned by a `Drop` guard however `inspect` returns.
  - `blender_path()` resolves `M2M_BLENDER` first (so CI or another machine points at its own install), then the macOS default, and never assumes a path exists without checking.
  - **Verified end to end against real Blender** (the `#[ignore]`d live test, run this session): imported `rig-human.glb`, got 66 bones, 0 meshes, 1 armature. CI cannot run Blender, so parsing is CI-tested (success, failure-with-only-error-fields, and garbage → `BadReport` not a panic) and the live spawn is `#[ignore]`d.
  - Mutations 3/3 (bad-report-is-an-error, mesh_vertices actually carried, and a fixture-range data mutation the frame-range assertion caught).
- [ ] **P4-1b** `m2m-bridge` LIVE mode — attach to a running Blender via a companion add-on for artist round-tripping (`architecture.md` §6). Bigger; needs the add-on.
- [ ] **P4-2** Blender add-on for live round-trip
- [x] **P4-3** Visual regression harness (structural): `src-tauri/tests/visual_regression.rs` fits+binds+exports every one of the 9 templates and reads the .glb back through Blender (`m2m_bridge::inspect`), asserting a committed per-creature baseline (bone count, welded vertex count) plus reader-independent invariants (imports as 1 mesh, 0 unweighted vertices, weight_total == vertex count). #[ignore]d — CI has no Blender; run `cargo test -p mesh2motion --release -- --ignored`. Baselines recorded + all 9 pass (2e501e1). First automated proof the full rig works for all 9 creatures, not just human. Follow-up (optional): image-pose pixel-diff variant — heavier, flaky across Blender/GPU versions, deferred until a numeric regression proves insufficient.
- [~] **P4-4** Performance pass vs `test.md` §6 — **MEASURED (session 061), within budget with large margin; no optimization warranted.** Release, avg of 5, on the available creatures (max human 7399 verts; no 50k-vert model exists to hit the budget's stated size). human: fit(incl. GLB read) 14.1ms, bind 26.7ms, export 26.6ms. Linear extrapolation to 50k: load ~95ms (budget 400ms), bind ~180ms (budget 3s — ≤1.2s even if O(nÂ²)), export ~180ms (budget 2s) — 4-16x headroom. Binary 7.81 MB (budget 40 MB). NOT profiled: peak RSS (needs Instruments + a 50k model; given KB-MB vertex data vs GB budgets, not a concern but unmeasured) — left [~] not [x] for that reason. Idle-CPU 0% is P4-5 (event-driven render, done).
- [x] **P4-5** DONE (verified session 063): event-driven render, no standing rAF loop — Idle CPU → 0% (event-driven render, no always-on rAF loop)
- [ ] **P4-6** *(optional, gated on R-7)* UniRig ONNX opt-in path via `ort` + CoreML EP
- [~] **P4-7** Release workflow DELIVERED (session 063): .github/workflows/release.yml builds+signs+notarises+publishes a GitHub release on a v* tag via tauri-action, secrets documented inline. Signing itself is BLOCKED on an external input — the user must add their Apple Developer cert + credentials as repo secrets (APPLE_CERTIFICATE etc.); until then the workflow builds an UNSIGNED bundle (degrades gracefully). Left [~]: the code is done, notarised signing awaits the cert.
- [x] **P4-8** `README.md` rewrite (af66d29): status block updated scaffold→"core rigging flow complete" for all 9 templates; solver-replacement claim moved future→present (implemented, every vertex binds with full weight); added a six-step table (import→skeleton→fit→bind→animate→export) each row naming its Tauri command, a Blender-bridge section (optional, the independent reader), and an #[ignore]d/Blender note in "Run the checks". Quick-start commands + crate layout + links all verified against the tree. Sonar gate deliberately omitted from the public README — internal-only (local docker + gitignored token), not part of clone→running.
- [x] **P4-9** DONE (session 063): user guide at docs/user-guide.md (six-step walkthrough incl. the new joint-drag gizmo, tips/gotchas, Blender-check note); in-app guidance strip (app/src/state/steps.ts) complete for all six steps and updated for the gizmo.
- [ ] **P4-Q** SonarQube `rust:S3776` cognitive-complexity smells (23 flagged, existing-code not gate-failing). **6 of 23 done** (f461c0d, c3cd7bb, b18577a): the two worst GLB parser fns — `glb::read::check_indices` 41→(split into `check_index`/`json_array`/`Counts`+8 per-entity validators) and `glb::write::write` 35→(split into `write_primitive`/`vec4_attribute`/`write_skins`/`write_nodes`). Behaviour-preserving; guard = m2m-io 208/208 both profiles + clippy/fmt + Blender visual-regression baselines identical. Done: glb check_indices(41), glb write(35), glb read_primitives(51), skin bind(29), animation curve_nodes(23), animation vector_track(20). Remaining worst: fbx/text.rs:209 (62 — subtle line-by-line state machine, HIGHEST risk, do with a ParserState struct), fbx/geometry.rs:391 (54), glb/mod.rs:761 (51), fbx/animation.rs:557 (25)/:422 (20), fbx/skin.rs (other). Bounded subsets; tests/examples ones low priority.

---

## Changed requirements

*(none yet — strike through and explain here when scope changes)*

## Asset issues found (not solver bugs)

- **`rig-shark.glb`'s `back_fin_2_l/r` bones sit outside `model-shark.glb`.** Indices 13 and 17, a mirrored pair at x = ±0.451, z = −1.83. Found by `unreachable_bones()` during P1-8 and pinned as an expected value in the A/B so a *new* unreachable bone anywhere fails the test. Harmless in the app — the user fits the skeleton before binding — but the shipped template does not fit its own model there.

## Blocked / needs user input

- **`references/` licensing** — the 7 Mixamo FBX files are gitignored for now: royalty-free to use but not CC0, and this repo licenses all art as CC0. Confirm whether to commit them anyway, keep them local, or replace with CC0 equivalents (R-5).

## Deferred with reason

- **Windows/Linux builds** — macOS-native first per `project_context.md` non-goals. Do not add cross-platform abstraction speculatively.
- **Neural rigging as core** — ADR A5: ~1.5 GB RAM + ORT-from-source conflicts with the resource budget. Revisit at P4-6.
